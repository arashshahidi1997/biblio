# Configuration

Primary config files:

- `bib/config/biblio.yml`
- `bib/config/citekeys.md`
- `bib/config/rag.yaml`

`biblio.yml` controls:

- BibTeX merge behavior
- PDF fetch layout
- Docling execution
- OpenAlex output locations

`rag.yaml` holds bibliography-owned RAG source definitions.
