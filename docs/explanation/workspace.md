# Workspace Model

`biblio` is built around a repo-local `bib/` workspace.

That keeps:

- bibliography inputs close to the project
- derivatives reproducible and inspectable
- project-specific notes and summaries in one place
- site generation local and disposable
