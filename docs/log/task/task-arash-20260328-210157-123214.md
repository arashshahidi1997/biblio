---
title: "GUI: Concept viewer, comparison, and reading list panels"
date: 2026-03-28
timestamp: 20260328-210157-123214
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260328-213658-d82db7/note.md
completed: 2026-03-28T21:37:00+01:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260328-205729-260870.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# GUI: Concept viewer, comparison, and reading list panels

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read `frontend/src/components/PaperTab.jsx` (sidebar panels, lines 786-825), `frontend/src/components/ExploreTab.jsx` (paper list, filtering, context menu), and `frontend/src/App.jsx` (context menu state, tab management).
> Read `src/biblio/ui.py` for the concepts, comparison, and reading list API endpoints.
>
> Implement the following:
>
> ### 1. Concepts panel in PaperTab sidebar
> - Add a "Concepts" panel below LibraryPanel in the right sidebar
> - Fetch `GET /api/papers/{citekey}/concepts` when PaperTab loads
> - Display concepts as grouped tag pills: Methods, Datasets, Metrics, Domains, Techniques
> - Each concept pill is clickable — clicking navigates to a concept search showing all papers with that concept
> - If no concepts extracted yet, show an "Extract Concepts" button that triggers `POST /api/actions/concepts-extract`
>
> ### 2. Comparison panel
> Add a comparison workflow accessible from ExploreTab:
> - Allow multi-select of papers in ExploreTab (checkboxes or shift-click)
> - When 2+ papers are selected, show a "Compare Selected" button in the toolbar
> - Clicking it opens a comparison panel/modal that:
>   - Calls `POST /api/actions/compare` with the selected citekeys
>   - Shows a loading state while the background action runs (poll status)
>   - Renders the resulting markdown comparison table using `renderMarkdown`
>   - Optionally allows choosing comparison dimensions before generating
> - Also add "Compare" to the paper context menu (right-click) — adds the paper to a comparison selection set
>
> ### 3. Reading list tool
> Add a "Reading List" section to ExploreTab (or as a collapsible panel):
> - Text input for research question
> - "Generate Reading List" button
> - Calls `POST /api/reading-list` with the question
> - Displays ranked results: each entry shows citekey, relevance score (as a bar or badge), and the justification text
> - Clicking a result opens the paper in PaperTab
> - "Set Priority" quick action per result that calls `POST /api/library/{citekey}` to update priority
>
> ### 4. Concept search
> Add concept search to SearchTab or as a section in ExploreTab:
> - Input field for concept search
> - Calls `POST /api/concepts/search`
> - Shows results grouped by concept with matching papers listed
>
> Follow existing component and CSS patterns. After all changes, use `datalad_save` MCP tool to commit with message: "Add concept viewer, comparison panel, and reading list to GUI".

---
Source: docs/log/task/task-arash-20260328-205729-260870.md

# GUI: Concept viewer, comparison, and reading list panels

## Goal

Add concept display to PaperTab sidebar, a multi-paper comparison panel, and a reading list curation tool accessible from the ExploreTab or as a new top-level tab.

## Context

- PaperTab right sidebar currently has: LibraryPanel, Related local papers, Neighborhood
- Concepts are per-paper YAML: methods, datasets, metrics, domains, techniques
- Comparison requires selecting multiple papers — ExploreTab already supports context menus on papers
- Reading list takes a research question and returns ranked unread papers
- Backend endpoints: `GET /api/papers/{citekey}/concepts`, `GET /api/concepts/index`, `POST /api/concepts/search`, `POST /api/actions/compare`, `POST /api/reading-list`
- The app has a context menu system (`contextMenu` state in App.jsx)

## Prompt

> Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read `frontend/src/components/PaperTab.jsx` (sidebar panels, lines 786-825), `frontend/src/components/ExploreTab.jsx` (paper list, filtering, context menu), and `frontend/src/App.jsx` (context menu state, tab management).
> Read `src/biblio/ui.py` for the concepts, comparison, and reading list API endpoints.
>
> Implement the following:
>
> ### 1. Concepts panel in PaperTab sidebar
> - Add a "Concepts" panel below LibraryPanel in the right sidebar
> - Fetch `GET /api/papers/{citekey}/concepts` when PaperTab loads
> - Display concepts as grouped tag pills: Methods, Datasets, Metrics, Domains, Techniques
> - Each concept pill is clickable — clicking navigates to a concept search showing all papers with that concept
> - If no concepts extracted yet, show an "Extract Concepts" button that triggers `POST /api/actions/concepts-extract`
>
> ### 2. Comparison panel
> Add a comparison workflow accessible from ExploreTab:
> - Allow multi-select of papers in ExploreTab (checkboxes or shift-click)
> - When 2+ papers are selected, show a "Compare Selected" button in the toolbar
> - Clicking it opens a comparison panel/modal that:
>   - Calls `POST /api/actions/compare` with the selected citekeys
>   - Shows a loading state while the background action runs (poll status)
>   - Renders the resulting markdown comparison table using `renderMarkdown`
>   - Optionally allows choosing comparison dimensions before generating
> - Also add "Compare" to the paper context menu (right-click) — adds the paper to a comparison selection set
>
> ### 3. Reading list tool
> Add a "Reading List" section to ExploreTab (or as a collapsible panel):
> - Text input for research question
> - "Generate Reading List" button
> - Calls `POST /api/reading-list` with the question
> - Displays ranked results: each entry shows citekey, relevance score (as a bar or badge), and the justification text
> - Clicking a result opens the paper in PaperTab
> - "Set Priority" quick action per result that calls `POST /api/library/{citekey}` to update priority
>
> ### 4. Concept search
> Add concept search to SearchTab or as a section in ExploreTab:
> - Input field for concept search
> - Calls `POST /api/concepts/search`
> - Shows results grouped by concept with matching papers listed
>
> Follow existing component and CSS patterns. After all changes, use `datalad_save` MCP tool to commit with message: "Add concept viewer, comparison panel, and reading list to GUI".

## Acceptance Criteria

- [ ] Concepts panel in PaperTab sidebar with grouped tag pills
- [ ] Concept pills clickable for cross-paper search
- [ ] Multi-select comparison workflow in ExploreTab
- [ ] Comparison table rendered as markdown
- [ ] Reading list tool with question input and ranked results
- [ ] Concept search functional
- [ ] Changes committed via `datalad_save`

## Result

(Filled in after execution)

## Acceptance Criteria

- [ ] 

## Result

Completed in 387.3s (1 steps, model: opus)

Session: `8ff02a40-ec55-4d2d-89bc-534a80d134c0`
Result note: `/storage2/arash/worklog/workflow/captures/20260328-213658-d82db7/note.md`

### Output

```
All changes saved. Here's a summary of what was implemented:

### 1. Concepts Panel (`PaperTab.jsx`)
- `ConceptsPanel` component below `LibraryPanel` in the right sidebar
- Fetches `GET /api/papers/{citekey}/concepts` on load
- Displays concepts as color-coded pills grouped by: methods, datasets, metrics, domains, techniques
- Clickable pills trigger concept search via `onConceptClick` → opens search panel
- "Extract Concepts" button shown when no concepts exist

### 2. Comparison Panel (`App.jsx`)
- `compareSelection` state tracks selected citekeys for comparison
- "Add to comparison" option in the paper context menu (right-click)
- Comparison bar appears when 1+ papers selected, showing pills with remove buttons
- "Compare Selected" button triggers `POST /api/actions/compare` with polling
- Modal overlay renders the comparison markdown result via `renderMarkdown`
- Added `GET /api/comparisons/latest` backend endpoint to serve the latest comparison file

### 3. Reading List (`App.jsx`)
- Toggle via ☰ icon in the sidebar
- Text input for research question + "Generate Reading List" button
- Calls `POST /api/reading-list`, polls `GET /api/actions/reading-list/status`
- Results show citekey, relevance score bar, justification text
- Clicking a result opens it in PaperTab
- "★ High" quick-action sets priority via `POST /api/library/{citekey}`

### 4. Concept Search (`App.jsx`)
- Triggered by clicking concept pills in PaperTab
- Opens the search sidebar and calls `POST /api/concepts/search`
- Results grouped by matching concept, each showing clickable citekey chips
- Close button to dismiss results

### CSS
- Styled all new components: concept pills (per-category colors), comparison bar/modal, reading list panel with score bars, concept search results

## Next Steps
1. Install frontend node_modules and run `make build-frontend` to compile the static assets for production use.
2. Add multi-select checkboxes directly in the CorpusTab paper list rows for easier comparison s
```

