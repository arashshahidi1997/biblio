---
title: "Paper notes editor in PaperTab"
date: 2026-03-29
timestamp: 20260329-041423-824361
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260329-045229-92bc20/note.md
completed: 2026-03-29T04:52:30+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041124-658920.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Paper notes editor in PaperTab

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/components/PaperTab.jsx` — the `CONTENT_SUBTABS` array and how subtab content is rendered. Read `src/biblio/library.py` for the `notes_path()` function. Read `frontend/src/utils/markdown.js` for markdown rendering.
>
> Implement:
> 1. **Backend endpoints**:
>    - `GET /api/papers/{citekey}/notes` — returns the raw markdown content of `bib/notes/{citekey}.md`, or empty string if not exists
>    - `PUT /api/papers/{citekey}/notes` — saves markdown content to `bib/notes/{citekey}.md`, creates the file/directory if needed
> 2. **Notes subtab** — add "Notes" to `CONTENT_SUBTABS` in PaperTab
>    - Split view: left side is a textarea editor, right side is rendered markdown preview (toggle between edit/preview/split)
>    - Auto-save on blur or after 2 seconds of inactivity (debounced)
>    - Show "Saved" / "Unsaved changes" indicator
>    - If no notes exist yet, show a friendly "Start writing notes..." placeholder
> 3. **Notes badge** — add an `ArtifactBadge` for notes in the paper header showing whether notes exist
> 4. The site builder in `src/biblio/site.py` already scans for `*notes*` — verify it picks these up
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add paper notes editor to PaperTab".

---
Source: docs/log/task/task-arash-20260329-041124-658920.md

# Paper notes editor in PaperTab

## Goal

Add a "Notes" subtab to PaperTab for editing per-paper markdown notes stored at `bib/notes/{citekey}.md`.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/components/PaperTab.jsx` — the `CONTENT_SUBTABS` array and how subtab content is rendered. Read `src/biblio/library.py` for the `notes_path()` function. Read `frontend/src/utils/markdown.js` for markdown rendering.
>
> Implement:
> 1. **Backend endpoints**:
>    - `GET /api/papers/{citekey}/notes` — returns the raw markdown content of `bib/notes/{citekey}.md`, or empty string if not exists
>    - `PUT /api/papers/{citekey}/notes` — saves markdown content to `bib/notes/{citekey}.md`, creates the file/directory if needed
> 2. **Notes subtab** — add "Notes" to `CONTENT_SUBTABS` in PaperTab
>    - Split view: left side is a textarea editor, right side is rendered markdown preview (toggle between edit/preview/split)
>    - Auto-save on blur or after 2 seconds of inactivity (debounced)
>    - Show "Saved" / "Unsaved changes" indicator
>    - If no notes exist yet, show a friendly "Start writing notes..." placeholder
> 3. **Notes badge** — add an `ArtifactBadge` for notes in the paper header showing whether notes exist
> 4. The site builder in `src/biblio/site.py` already scans for `*notes*` — verify it picks these up
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add paper notes editor to PaperTab".

## Acceptance Criteria

- [ ] 

## Result

Completed in 200.2s (1 steps, model: opus)

Session: `892ea945-20c1-402f-8ac1-c55b97181df5`
Result note: `/storage2/arash/worklog/workflow/captures/20260329-045229-92bc20/note.md`

### Output

```
Done. Here's what was implemented:

**Backend** (`src/biblio/ui.py`):
- `GET /api/papers/{citekey}/notes` — returns raw markdown content (empty string if no file)
- `PUT /api/papers/{citekey}/notes` — saves markdown to `bib/notes/{citekey}.md`, creates directory if needed
- Added `has_notes` flag to the `/api/model` paper payload

**Frontend** (`PaperTab.jsx`):
- **NotesTab** component with split/edit/preview toggle views
- Left side: textarea editor with placeholder "Start writing notes..."
- Right side: rendered markdown preview using existing `renderMarkdown`
- Auto-save: debounced at 2 seconds of inactivity + saves on blur
- Status indicator: "Saved" (green) / "Unsaved changes" (amber) / "Saving…" (blue)
- Added "Notes" to `CONTENT_SUBTABS`
- Added `ArtifactBadge` for notes in the paper header

**CSS** (`App.css`): Styles for notes tab layout, split view, editor, and preview pane

**Site builder** (`site.py`): Already scans for `*note*` files in docling output dirs — the `bib/notes/` files are user notes at a separate path, which is the correct design.

**Tests**: 157 passed, 0 failed.

## Next Steps
1. Add a test for the notes GET/PUT endpoints (e.g. `tests/test_ui_notes.py` with FastAPI TestClient).
2. Add keyboard shortcut (Ctrl+S) to manually trigger save in the notes editor.
3. Wire the notes badge click to open the Notes subtab directly (convenience navigation).
```

