---
title: "Duplicate ingestion guard on DOI"
date: 2026-03-29
timestamp: 20260329-041419-108879
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260329-044336-53c1a0/note.md
completed: 2026-03-29T04:43:38+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041123-417130.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Duplicate ingestion guard on DOI

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/ingest.py` thoroughly — understand the ingest pipeline, how DOIs are resolved, and where entries are written. Read `src/biblio/citekeys.py` to understand how citekeys are tracked. Read `src/biblio/mcp.py` for `ingest_dois`.
>
> Implement:
> 1. In the ingest pipeline, before making any network call (OpenAlex/CrossRef), check if the DOI already exists in any BibTeX source file or if the resulting citekey is already registered
> 2. Return a clear status: `"skipped_duplicate"` with the existing citekey
> 3. For `biblio ingest --dois`, report: "3 added, 2 skipped (already in library)"
> 4. Add a `--force` flag to re-ingest even if duplicate detected
> 5. For the MCP tool `ingest_dois`, include skip info in the response
> 6. Add tests for duplicate detection
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add duplicate ingestion guard on DOI".

---
Source: docs/log/task/task-arash-20260329-041123-417130.md

# Duplicate ingestion guard on DOI

## Goal

Prevent re-ingesting papers that already exist in the library by checking DOI/citekey before network calls.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/ingest.py` thoroughly — understand the ingest pipeline, how DOIs are resolved, and where entries are written. Read `src/biblio/citekeys.py` to understand how citekeys are tracked. Read `src/biblio/mcp.py` for `ingest_dois`.
>
> Implement:
> 1. In the ingest pipeline, before making any network call (OpenAlex/CrossRef), check if the DOI already exists in any BibTeX source file or if the resulting citekey is already registered
> 2. Return a clear status: `"skipped_duplicate"` with the existing citekey
> 3. For `biblio ingest --dois`, report: "3 added, 2 skipped (already in library)"
> 4. Add a `--force` flag to re-ingest even if duplicate detected
> 5. For the MCP tool `ingest_dois`, include skip info in the response
> 6. Add tests for duplicate detection
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add duplicate ingestion guard on DOI".

## Acceptance Criteria

- [ ] 

## Result

Completed in 714.2s (1 steps, model: opus)

Session: `062266c6-eac5-469d-a5d2-f55d07db00af`
Result note: `/storage2/arash/worklog/workflow/captures/20260329-044336-53c1a0/note.md`

### Output

```
All done. Here's what was implemented:

**`src/biblio/ingest.py`**:
- Added `find_existing_dois()` — scans all `bib/srcbib/*.bib` files and returns a normalized DOI → citekey mapping
- Extended `IngestResult` with a `skipped` field (tuple of `(doi, existing_citekey)` pairs)
- Added `force` parameter to `ingest_file()` — when `False` (default), DOIs already in srcbib are filtered out *before* any network calls (OpenAlex/CrossRef); works for `dois`, `csljson`, and `ris` source types

**`src/biblio/cli.py`**:
- Added `--force` flag to all ingest subcommands (`dois`, `csljson`, `ris`, `pdfs`)
- Updated output to report: `"3 added, 2 skipped (already in library)"` with per-DOI skip details

**`src/biblio/mcp.py`**:
- Added `force` parameter to `ingest_dois()`
- Return dict now includes `skipped` (list of `{doi, existing_citekey}`) and `skipped_count`

**`tests/test_biblio_ingest.py`** — 6 new tests:
- `test_find_existing_dois_discovers_doi_in_srcbib`
- `test_find_existing_dois_empty_when_no_srcbib`
- `test_ingest_dois_skips_duplicate_doi` (verifies OpenAlex not called for skipped DOI)
- `test_ingest_dois_force_reingests_duplicate`
- `test_ingest_csljson_skips_duplicate_doi`
- `test_ingest_skipped_field_default_empty`

**Tests**: 240 passed, 0 failed.

## Next Steps
(done)
```

