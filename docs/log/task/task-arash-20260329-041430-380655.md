---
title: "Duplicate paper detection and lint report"
date: 2026-03-29
timestamp: 20260329-041430-380655
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260329-052104-75aa60/note.md
completed: 2026-03-29T05:21:06+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041125-994667.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Duplicate paper detection and lint report

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/bibtex.py`, `src/biblio/citekeys.py`, and `src/biblio/library.py` to understand how paper metadata is stored. Read `src/biblio/tag_vocab.py` for the existing lint pattern (`lint_library_tags`).
>
> Implement:
> 1. **Duplicate detection module** — create `src/biblio/dedup.py` with:
>    - `find_duplicates(repo_root)` — scans all BibTeX entries and returns groups of likely duplicates, detected by:
>      - Exact DOI match (different citekeys, same DOI)
>      - Normalized title similarity (case-insensitive, strip punctuation, >90% match)
>      - Same OpenAlex ID (if resolved)
>    - Each group: `{citekeys: [...], reason: "doi"|"title"|"openalex", confidence: float, suggested_keep: str}`
>    - `suggested_keep` picks the citekey with most derivatives/metadata
> 2. **CLI** — `biblio library dedup [--json]` that prints the duplicate report
> 3. **MCP tool** — `biblio_dedup()`
> 4. **API endpoint** — `POST /api/library/dedup` returning the duplicate groups as JSON
> 5. **GUI** — add a "Duplicates" section in ActionsTab (alongside the Tag Health lint). Show duplicate groups with "Merge" button per group. Merging keeps the suggested citekey and removes the other(s), transferring any unique tags/notes.
> 6. **Tests** — test duplicate detection with mock BibTeX data
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add duplicate paper detection and lint report".

---
Source: docs/log/task/task-arash-20260329-041125-994667.md

# Duplicate paper detection and lint report

## Goal

Detect duplicate papers in the library (shared DOI, near-identical titles, same OpenAlex ID) and present a lint-style report with merge suggestions.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/bibtex.py`, `src/biblio/citekeys.py`, and `src/biblio/library.py` to understand how paper metadata is stored. Read `src/biblio/tag_vocab.py` for the existing lint pattern (`lint_library_tags`).
>
> Implement:
> 1. **Duplicate detection module** — create `src/biblio/dedup.py` with:
>    - `find_duplicates(repo_root)` — scans all BibTeX entries and returns groups of likely duplicates, detected by:
>      - Exact DOI match (different citekeys, same DOI)
>      - Normalized title similarity (case-insensitive, strip punctuation, >90% match)
>      - Same OpenAlex ID (if resolved)
>    - Each group: `{citekeys: [...], reason: "doi"|"title"|"openalex", confidence: float, suggested_keep: str}`
>    - `suggested_keep` picks the citekey with most derivatives/metadata
> 2. **CLI** — `biblio library dedup [--json]` that prints the duplicate report
> 3. **MCP tool** — `biblio_dedup()`
> 4. **API endpoint** — `POST /api/library/dedup` returning the duplicate groups as JSON
> 5. **GUI** — add a "Duplicates" section in ActionsTab (alongside the Tag Health lint). Show duplicate groups with "Merge" button per group. Merging keeps the suggested citekey and removes the other(s), transferring any unique tags/notes.
> 6. **Tests** — test duplicate detection with mock BibTeX data
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add duplicate paper detection and lint report".

## Acceptance Criteria

- [ ] 

## Result

Completed in 812.4s (1 steps, model: opus)

Session: `1aab5521-de64-437a-a495-0c4d2861c7ce`
Result note: `/storage2/arash/worklog/workflow/captures/20260329-052104-75aa60/note.md`

### Output

```
All done. Here's what was implemented:

**Core module** — `src/biblio/dedup.py` with `find_duplicates(repo_root)` detecting duplicates by:
- Exact DOI match (normalized, URL-stripped, case-insensitive)
- Normalized title similarity (>90% via SequenceMatcher, accent-stripped, punctuation-removed)
- Same OpenAlex ID (from resolve JSONL)
- Each group includes `suggested_keep` (citekey with most derivatives/metadata)

**CLI** — `biblio library dedup [--json]` prints human-readable or JSON duplicate report

**MCP** — `library_dedup(root)` in `biblio/mcp.py` returns `{groups, count}`. Note: the projio MCP server registration (`src/projio/mcp/biblio.py` and `server.py`) couldn't be written due to file permissions — that needs a separate edit in the parent package.

**API** — Two endpoints:
- `POST /api/library/dedup` — returns duplicate groups
- `POST /api/library/dedup/merge` — merges duplicates (keeps suggested, removes others, transfers tags/notes, updates citekeys.md)

**GUI** — `DedupPanel` component in ActionsTab with "Detect Duplicates" button and per-group "Merge" button

**Tests** — 10 tests in `tests/test_dedup.py` covering DOI duplicates, DOI normalization, title similarity, threshold tuning, suggested_keep scoring, OpenAlex ID duplicates, missing bib, and helper functions. All 171 tests pass.

## Next Steps
1. Register `biblio_dedup` in the projio MCP server (`src/projio/mcp/biblio.py` and `server.py`) so it's exposed as an MCP tool.
2. Build the frontend bundle with `make build-frontend` to include the DedupPanel in the static assets.
3. Add a merge action that also removes the duplicate's BibTeX entry from srcbib files and cleans up derivative directories.
```

