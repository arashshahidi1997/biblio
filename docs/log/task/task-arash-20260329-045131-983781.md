---
title: "Fix graph: expanded refs in focused view, persistent green selection, expand-all confirmation"
date: 2026-03-29
timestamp: 20260329-045131-983781
status: pending
actionable: true
prompt: ""
project_primary: "biblio"
tags: [task, gui, graph, bugfix]
series: biblio-polish
---

# Fix graph: expanded refs in focused view, persistent green selection, expand-all confirmation

## Goal

Fix three graph visualization bugs: (1) expanded references not showing in focused neighborhood view, (2) selected nodes turning green/teal and staying that color persistently, (3) "Expand Graph" for all papers needs a confirmation dialog.

## Context

The graph is rendered using Cytoscape.js in `frontend/src/components/ExploreTab.jsx`.

### Bug 1: Focused view doesn't show expanded graph
- User selects a paper, switches to "Focused" view, clicks "Expand Graph" for that paper
- Backend responds "Graph expanded: 77 total candidates"
- But the focused neighborhood view still shows the same nodes — the expanded candidates don't appear
- Root cause likely: after graph-expand completes, the graph data in the model isn't refreshed, OR the focused mode filter doesn't include the newly expanded candidates from the graph-candidates API
- The whole point of focused view + expand is to see a paper's references as nodes and be able to ingest them

### Bug 2: Selected nodes turn green and persist
- Clicking a node makes it teal/green (#1a9e8a) and it stays that color even after clicking elsewhere
- Root cause in ExploreTab.jsx around line 243-248: `node[?active]` style applies `background-color: #1a9e8a` based on a data attribute `active`. The `tap` handler (line 282-298) calls `evt.target.select()` which sets Cytoscape's `:selected` pseudo-class
- The `active` data attribute is likely being set but never cleared when another node is clicked or when clicking the canvas
- The `:selected` style (line 251-261) adds blue shadow — that's fine — but the `active` data flag persists

### Bug 3: Expand Graph for All needs confirmation
- "Expand Graph" button in ActionsTab triggers for all papers with no confirmation
- This is a heavy operation (one OpenAlex API call per paper, rate-limited)
- Should show confirmation: "This will query OpenAlex for N papers. This may take several minutes. Proceed?"

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read these files thoroughly:
> - `frontend/src/components/ExploreTab.jsx` — the full Cytoscape graph setup, especially:
>   - Lines 220-278: Cytoscape stylesheet (the `node[?active]` style at line 243 is the green bug)
>   - Lines 282-312: event handlers (tap, dbltap — how `active` and `select` are managed)
>   - The graph data assembly: how nodes/edges are built from `payload.graph` and the focused mode filter
>   - How/when graph data is refreshed after actions complete
> - `frontend/src/App.jsx` — how `graphMode` ("all" | "focused") works, how `triggerAction("graph-expand")` is called, and how model data is reloaded after actions
> - `src/biblio/ui.py` — the `/api/graph` endpoint, the `/api/graph-candidates` endpoint, and the `graph-expand` action endpoint. Understand what data is returned and how focused vs all modes differ.
> - `frontend/src/components/ActionsTab.jsx` — the "Expand Graph" button
>
> Fix all three issues:
>
> ### Fix 1: Expanded graph showing in focused view
> After `graph-expand` completes for a paper:
> - The graph data must be refreshed — call `loadModel()` or specifically reload graph data
> - In focused mode, the newly expanded candidates (outgoing/incoming references from OpenAlex) must appear as nodes in the Cytoscape graph
> - Verify the focused mode filter includes both existing local papers AND external candidates from the expand result
> - The expanded candidate nodes should be visually distinct from local papers (different color/shape, as external nodes)
> - Each external candidate node should be clickable: single-click shows info in Inspector, right-click or double-click offers "Add to Library" action
> - After expand, auto-fit the graph view to show all new nodes
>
> ### Fix 2: Persistent green/teal selection
> - The `node[?active]` style sets permanent teal color via a data flag. This flag is never cleared when deselecting.
> - Fix: when a node tap handler fires, first clear the `active` data flag from ALL nodes, then set it only on the newly tapped node
> - When tapping the canvas (background), clear `active` from all nodes
> - The `:selected` pseudo-class should also be properly cleared — the current code calls `cyRef.current.elements().unselect()` on canvas tap (line 304) which is correct, but the `active` data attribute persists independently
> - Ensure the visual feedback cycle is: tap node → node gets highlighted → tap different node → previous node returns to normal color → new node highlighted → tap canvas → all nodes return to normal
>
> ### Fix 3: Expand Graph confirmation
> - Wrap the "Expand Graph" button in ActionsTab with a confirmation dialog
> - When clicking "Expand Graph" (the all-papers version), show: "This will query OpenAlex for {N} papers. This may take several minutes. Proceed?" with [Expand] [Cancel]
> - The per-paper expand (from PaperTab or Inspector) does NOT need confirmation (it's a single paper)
> - Also show progress during the expand-all: "Expanding: 15/42 papers..."
>
> After all changes, run `make test`. Then use `datalad_save` MCP tool to commit with message: "Fix graph: show expanded refs in focused view, fix persistent selection color, add expand-all confirmation".
