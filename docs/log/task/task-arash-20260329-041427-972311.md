---
title: "BibTeX export for selected papers"
date: 2026-03-29
timestamp: 20260329-041427-972311
status: scheduled
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041125-547502.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# BibTeX export for selected papers

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/bibtex.py` and `src/biblio/_pybtex_utils.py` to understand BibTeX handling. Read `src/biblio/cli.py` for the bibtex subcommand group. Read `frontend/src/components/ExploreTab.jsx` for the paper list.
>
> Implement:
> 1. **Backend** — add `src/biblio/bibtex_export.py` with `export_bibtex(citekeys, repo_root) -> str` that reads entries from merged BibTeX and returns formatted `.bib` content for the requested citekeys
> 2. **API endpoint** — `POST /api/export/bibtex` accepting `{citekeys: [...]}`, returns the `.bib` content as a downloadable file (`Content-Disposition: attachment; filename="export.bib"`)
> 3. **CLI** — `biblio bibtex export [citekeys...] [--all] [--status STATUS] [--collection NAME] -o output.bib`
> 4. **MCP tool** — `biblio_export_bibtex(citekeys)`
> 5. **GUI** — add "Export BibTeX" button that appears when papers are selected in ExploreTab (or in the bulk action toolbar from task 3). Also add it as a context menu option. Triggers browser download of the `.bib` file.
> 6. Also add "Copy BibTeX" that copies the `.bib` content to clipboard instead of downloading
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add BibTeX export for selected papers".

---
Source: docs/log/task/task-arash-20260329-041125-547502.md

# BibTeX export for selected papers

## Goal

Add BibTeX export: select papers in the GUI and download a `.bib` file. Also add a CLI command and MCP tool.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/bibtex.py` and `src/biblio/_pybtex_utils.py` to understand BibTeX handling. Read `src/biblio/cli.py` for the bibtex subcommand group. Read `frontend/src/components/ExploreTab.jsx` for the paper list.
>
> Implement:
> 1. **Backend** — add `src/biblio/bibtex_export.py` with `export_bibtex(citekeys, repo_root) -> str` that reads entries from merged BibTeX and returns formatted `.bib` content for the requested citekeys
> 2. **API endpoint** — `POST /api/export/bibtex` accepting `{citekeys: [...]}`, returns the `.bib` content as a downloadable file (`Content-Disposition: attachment; filename="export.bib"`)
> 3. **CLI** — `biblio bibtex export [citekeys...] [--all] [--status STATUS] [--collection NAME] -o output.bib`
> 4. **MCP tool** — `biblio_export_bibtex(citekeys)`
> 5. **GUI** — add "Export BibTeX" button that appears when papers are selected in ExploreTab (or in the bulk action toolbar from task 3). Also add it as a context menu option. Triggers browser download of the `.bib` file.
> 6. Also add "Copy BibTeX" that copies the `.bib` content to clipboard instead of downloading
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add BibTeX export for selected papers".

## Acceptance Criteria

- [ ] 

## Result

(pending)

