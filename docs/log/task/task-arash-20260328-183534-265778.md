---
title: "Concept extraction, comparison tables, and reading list curation"
date: 2026-03-28
timestamp: 20260328-183534-265778
status: scheduled
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260328-183238-827721.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Concept extraction, comparison tables, and reading list curation

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the three source idea notes referenced in Context above. Read `src/biblio/summarize.py` (from task 4), `src/biblio/library.py`, `src/biblio/cli.py`, and `src/biblio/mcp.py`.
>
> Implement the following three features:
>
> ### A. Concept extraction
> 1. Create `src/biblio/concepts.py` with `extract_concepts(citekey, root, prompt_only=False)` that extracts key concepts from a paper's summary/abstract and stores them as `bib/derivatives/concepts/{citekey}.yml`.
> 2. Schema: `{methods: [...], datasets: [...], metrics: [...], domains: [...], techniques: [...]}` — each entry is a short string.
> 3. Add `build_concept_index(root)` that aggregates all per-paper concept files into a cross-paper index: concept → list of citekeys.
> 4. CLI: `biblio concepts extract <citekey> [--all]` and `biblio concepts search <concept>`.
> 5. MCP: `biblio_concepts(citekey)` and `biblio_concept_search(query)`.
>
> ### B. Comparison tables
> 1. Add `compare(citekeys, root, dimensions=None, prompt_only=False)` to a new `src/biblio/compare.py`. Default dimensions: method, dataset, metrics, key findings, limitations.
> 2. Output: markdown table + optional prose synthesis. Store as `bib/derivatives/comparisons/{slug}.md` where slug is derived from the citekeys.
> 3. CLI: `biblio compare <citekey1> <citekey2> [... ] [--dimensions method,dataset] [--prompt-only]`.
> 4. MCP: `biblio_compare(citekeys, dimensions)`.
>
> ### C. Reading list curation
> 1. Add `reading_list(question, root, count=5, prompt_only=False)` to a new `src/biblio/reading_list.py`. Scores unread/queued papers by relevance to the question using summaries + concepts.
> 2. Output: ranked list with per-paper justification.
> 3. CLI: `biblio reading-list "<question>" [--count N] [--prompt-only]`.
> 4. MCP: `biblio_reading_list(question, count)`.
>
> Add tests for concept schema, comparison table generation, and reading list ranking logic. After all changes, run tests with `make test`. Then use `datalad_save` MCP tool to commit with message: "Add concept extraction, comparison tables, and reading list curation".

---
Source: docs/log/task/task-arash-20260328-183238-827721.md

# Concept extraction, comparison tables, and reading list curation

## Goal

Build three interrelated paper intelligence features: (1) structured concept extraction per paper, (2) multi-paper comparison tables, and (3) AI-prioritized reading list curation. All three build on the summary pipeline.

## Context

- Source ideas: concept extraction (`idea-arash-20260328-182618-137871`), comparison tables (`idea-arash-20260328-182618-656616`), reading list curation (`idea-arash-20260328-182618-279183`)
- Depends on task 4 (paper summaries) — needs at least abstracts/summaries to work with
- Concept extraction feeds into reading list scoring and comparison dimensions
- All three should support `--prompt-only` mode for non-LLM use

## Prompt

> Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the three source idea notes referenced in Context above. Read `src/biblio/summarize.py` (from task 4), `src/biblio/library.py`, `src/biblio/cli.py`, and `src/biblio/mcp.py`.
>
> Implement the following three features:
>
> ### A. Concept extraction
> 1. Create `src/biblio/concepts.py` with `extract_concepts(citekey, root, prompt_only=False)` that extracts key concepts from a paper's summary/abstract and stores them as `bib/derivatives/concepts/{citekey}.yml`.
> 2. Schema: `{methods: [...], datasets: [...], metrics: [...], domains: [...], techniques: [...]}` — each entry is a short string.
> 3. Add `build_concept_index(root)` that aggregates all per-paper concept files into a cross-paper index: concept → list of citekeys.
> 4. CLI: `biblio concepts extract <citekey> [--all]` and `biblio concepts search <concept>`.
> 5. MCP: `biblio_concepts(citekey)` and `biblio_concept_search(query)`.
>
> ### B. Comparison tables
> 1. Add `compare(citekeys, root, dimensions=None, prompt_only=False)` to a new `src/biblio/compare.py`. Default dimensions: method, dataset, metrics, key findings, limitations.
> 2. Output: markdown table + optional prose synthesis. Store as `bib/derivatives/comparisons/{slug}.md` where slug is derived from the citekeys.
> 3. CLI: `biblio compare <citekey1> <citekey2> [... ] [--dimensions method,dataset] [--prompt-only]`.
> 4. MCP: `biblio_compare(citekeys, dimensions)`.
>
> ### C. Reading list curation
> 1. Add `reading_list(question, root, count=5, prompt_only=False)` to a new `src/biblio/reading_list.py`. Scores unread/queued papers by relevance to the question using summaries + concepts.
> 2. Output: ranked list with per-paper justification.
> 3. CLI: `biblio reading-list "<question>" [--count N] [--prompt-only]`.
> 4. MCP: `biblio_reading_list(question, count)`.
>
> Add tests for concept schema, comparison table generation, and reading list ranking logic. After all changes, run tests with `make test`. Then use `datalad_save` MCP tool to commit with message: "Add concept extraction, comparison tables, and reading list curation".

## Acceptance Criteria

- [ ] Concept extraction stored as YAML per paper with cross-paper index
- [ ] Comparison tables output as markdown with configurable dimensions
- [ ] Reading list scores and ranks unread papers by relevance to a question
- [ ] All three support `--prompt-only` mode
- [ ] CLI subcommands: `biblio concepts`, `biblio compare`, `biblio reading-list`
- [ ] MCP tools: `biblio_concepts`, `biblio_concept_search`, `biblio_compare`, `biblio_reading_list`
- [ ] Tests pass
- [ ] Changes committed via `datalad_save`

## Result

(Filled in after execution)

## Acceptance Criteria

- [ ] 

## Result

(pending)

