---
title: "Bulk status and tag updates from ExploreTab"
date: 2026-03-29
timestamp: 20260329-041421-534165
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260329-044836-a308ea/note.md
completed: 2026-03-29T04:48:38+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041123-862189.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Bulk status and tag updates from ExploreTab

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/components/ExploreTab.jsx` (full file) to understand the paper list rendering, existing filtering, and context menu. Read `frontend/src/App.jsx` for state management. Read `src/biblio/ui.py` for `POST /api/library/{citekey}` and the MCP `library_set_bulk` pattern.
>
> Implement:
> 1. **Multi-select** — add checkboxes to each paper row in ExploreTab. Shift-click for range select. "Select all" / "Select none" controls.
> 2. **Bulk action toolbar** — appears when 1+ papers selected, showing:
>    - Status dropdown (unread/reading/processed/archived) → apply to all selected
>    - Priority dropdown (low/normal/high) → apply to all selected
>    - Tag input (add tags to all selected, with the TagInput autocomplete if available)
>    - "Remove tag" input (remove a specific tag from all selected)
>    - "Delete selected" button (with confirmation)
> 3. **Backend endpoint** — add `POST /api/library/bulk` that accepts `{citekeys: [...], status?, priority?, add_tags?, remove_tags?}` and updates all in one call
> 4. After bulk update, refresh the model data
>
> Follow existing CSS patterns. Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add bulk status and tag updates from ExploreTab".

---
Source: docs/log/task/task-arash-20260329-041123-862189.md

# Bulk status and tag updates from ExploreTab

## Goal

Add multi-select to ExploreTab paper list and a bulk action toolbar for setting status, priority, and tags on multiple papers at once.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/components/ExploreTab.jsx` (full file) to understand the paper list rendering, existing filtering, and context menu. Read `frontend/src/App.jsx` for state management. Read `src/biblio/ui.py` for `POST /api/library/{citekey}` and the MCP `library_set_bulk` pattern.
>
> Implement:
> 1. **Multi-select** — add checkboxes to each paper row in ExploreTab. Shift-click for range select. "Select all" / "Select none" controls.
> 2. **Bulk action toolbar** — appears when 1+ papers selected, showing:
>    - Status dropdown (unread/reading/processed/archived) → apply to all selected
>    - Priority dropdown (low/normal/high) → apply to all selected
>    - Tag input (add tags to all selected, with the TagInput autocomplete if available)
>    - "Remove tag" input (remove a specific tag from all selected)
>    - "Delete selected" button (with confirmation)
> 3. **Backend endpoint** — add `POST /api/library/bulk` that accepts `{citekeys: [...], status?, priority?, add_tags?, remove_tags?}` and updates all in one call
> 4. After bulk update, refresh the model data
>
> Follow existing CSS patterns. Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add bulk status and tag updates from ExploreTab".

## Acceptance Criteria

- [ ] 

## Result

Completed in 266.2s (1 steps, model: opus)

Session: `b1c4e66a-3ac5-4b02-8e6e-622812470cc4`
Result note: `/storage2/arash/worklog/workflow/captures/20260329-044836-a308ea/note.md`

### Output

```
All done. Here's a summary of what was implemented:

**Backend** (`src/biblio/library.py`):
- Added `bulk_update()` — updates multiple citekeys in a single library write, supporting `status`, `priority`, `add_tags`, and `remove_tags`

**Backend** (`src/biblio/ui.py`):
- Added `POST /api/library/bulk` endpoint accepting `{citekeys, status?, priority?, add_tags?, remove_tags?}`. Route registered before `/{citekey}` to avoid path variable collision

**Frontend** (`App.jsx`):
- Added `bulkSelection` state and `lastBulkClickRef` for shift-click range select
- Added `bulkUpdateLibrary()` — calls the bulk endpoint then refreshes model
- Added `toggleBulkSelect()` — handles single click and shift-click range selection
- Passed bulk props down to `CorpusTab`

**Frontend** (`CorpusTab.jsx`):
- Added checkbox column with "select all" header checkbox
- Rows highlight when selected (`.bulk-selected` class)
- Checkbox clicks stop propagation to avoid triggering row's `setActiveKey`
- **BulkToolbar** component appears when 1+ papers selected, with:
  - Status dropdown (unread/reading/processed/archived)
  - Priority dropdown (low/normal/high)
  - Tag add input (with TagInput autocomplete) + "Add" button
  - Tag remove input + "Remove" button
  - "Clear" button to deselect all

**CSS** (`App.css`):
- Styles for selected rows, checkboxes, bulk toolbar, tag sections, and action buttons following existing design tokens

**Tests**: 157 passed, 0 failed.

## Next Steps
1. Add backend tests for the `bulk_update` function and `POST /api/library/bulk` endpoint.
2. Add a "Delete selected" bulk action that calls `DELETE /api/papers/{citekey}` for each selected paper with a confirmation dialog.
3. Persist bulk selection across filter changes and add a floating selection count indicator when scrolled past the toolbar.
```

