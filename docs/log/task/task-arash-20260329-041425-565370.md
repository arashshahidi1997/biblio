---
title: "DOI clipboard paste detection in GUI"
date: 2026-03-29
timestamp: 20260329-041425-565370
status: scheduled
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041125-147182.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# DOI clipboard paste detection in GUI

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/App.jsx` to understand the top-level app structure. Read `frontend/src/components/ActionsTab.jsx` for the existing "Add paper by DOI" flow and `triggerAction("add-paper", {doi})`.
>
> Implement:
> 1. **Global paste listener** — add a `paste` event listener on the document in App.jsx
> 2. **DOI detection** — regex `10\.\d{4,}/\S+` to extract DOIs from pasted text. Also handle full URLs like `https://doi.org/10.xxxx/yyyy` and `https://dx.doi.org/10.xxxx/yyyy`
> 3. **Toast notification** — when a DOI is detected, show a small toast/banner at the top: "DOI detected: 10.xxxx/yyyy — [Add to library] [Dismiss]"
>    - "Add to library" calls `triggerAction("add-paper", {doi})`
>    - Toast auto-dismisses after 8 seconds
>    - Don't show if the DOI is already in the library (check against loaded papers)
>    - Don't trigger when pasting into input fields that already handle DOIs (the Add DOI input in ActionsTab)
> 4. **Multiple DOIs** — if paste contains multiple DOIs (e.g. pasting a reference list), show "N DOIs detected — [Add all] [Review]". "Review" opens a modal listing each DOI with checkboxes.
> 5. **CSS** — style the toast to match the app theme, positioned at top-center, with subtle animation
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add DOI clipboard paste detection to GUI".

---
Source: docs/log/task/task-arash-20260329-041125-147182.md

# DOI clipboard paste detection in GUI

## Goal

Detect DOI patterns on paste anywhere in the GUI and offer a quick "Add this paper?" action.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/App.jsx` to understand the top-level app structure. Read `frontend/src/components/ActionsTab.jsx` for the existing "Add paper by DOI" flow and `triggerAction("add-paper", {doi})`.
>
> Implement:
> 1. **Global paste listener** — add a `paste` event listener on the document in App.jsx
> 2. **DOI detection** — regex `10\.\d{4,}/\S+` to extract DOIs from pasted text. Also handle full URLs like `https://doi.org/10.xxxx/yyyy` and `https://dx.doi.org/10.xxxx/yyyy`
> 3. **Toast notification** — when a DOI is detected, show a small toast/banner at the top: "DOI detected: 10.xxxx/yyyy — [Add to library] [Dismiss]"
>    - "Add to library" calls `triggerAction("add-paper", {doi})`
>    - Toast auto-dismisses after 8 seconds
>    - Don't show if the DOI is already in the library (check against loaded papers)
>    - Don't trigger when pasting into input fields that already handle DOIs (the Add DOI input in ActionsTab)
> 4. **Multiple DOIs** — if paste contains multiple DOIs (e.g. pasting a reference list), show "N DOIs detected — [Add all] [Review]". "Review" opens a modal listing each DOI with checkboxes.
> 5. **CSS** — style the toast to match the app theme, positioned at top-center, with subtle animation
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add DOI clipboard paste detection to GUI".

## Acceptance Criteria

- [ ] 

## Result

(pending)

