---
title: "Batch ingest from BibTeX file with GUI preview"
date: 2026-03-29
timestamp: 20260329-160925-867604
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260329-165727-997110/note.md
completed: 2026-03-29T16:57:30+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041127-770661.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Batch ingest from BibTeX file with GUI preview

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/ingest.py` for the ingest pipeline. Read `src/biblio/_pybtex_utils.py` for BibTeX parsing. Read `src/biblio/cli.py` for existing `biblio ingest` subcommand (it already supports `--bib` for BibTeX files). Read `frontend/src/components/ActionsTab.jsx` for action patterns.
>
> Implement:
> 1. **Backend** — add `POST /api/ingest/preview-bib` that accepts a `.bib` file upload (multipart), parses it with pybtex, and returns a list of entries: `[{citekey, title, authors, year, doi, already_exists}]`. Check each entry against existing library for duplicate detection.
> 2. **Backend** — add `POST /api/ingest/import-bib` that accepts `{entries: [{citekey, ...}]}` (the selected subset) and ingests them, copying the BibTeX entries to `bib/srcbib/`. Use background action pattern with progress.
> 3. **Frontend** — add a drag-and-drop zone in ActionsTab (or a dedicated "Import" section):
>    - Drop a `.bib` file or click to upload
>    - Show preview table: checkbox, citekey, title, authors, year, DOI, status (new/duplicate)
>    - Duplicates are dimmed and unchecked by default
>    - "Select all new" / "Select none" controls
>    - "Import Selected" button → calls the import endpoint with checked entries
>    - Progress bar during import
> 4. Also support pasting raw BibTeX text into a textarea as an alternative to file upload
> 5. **Tests** — test preview parsing and duplicate detection
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add batch BibTeX import with GUI preview and drag-and-drop".

---
Source: docs/log/task/task-arash-20260329-041127-770661.md

# Batch ingest from BibTeX file with GUI preview

## Goal

Add drag-and-drop BibTeX file import to the GUI with a preview of entries before ingestion, and selective import.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/ingest.py` for the ingest pipeline. Read `src/biblio/_pybtex_utils.py` for BibTeX parsing. Read `src/biblio/cli.py` for existing `biblio ingest` subcommand (it already supports `--bib` for BibTeX files). Read `frontend/src/components/ActionsTab.jsx` for action patterns.
>
> Implement:
> 1. **Backend** — add `POST /api/ingest/preview-bib` that accepts a `.bib` file upload (multipart), parses it with pybtex, and returns a list of entries: `[{citekey, title, authors, year, doi, already_exists}]`. Check each entry against existing library for duplicate detection.
> 2. **Backend** — add `POST /api/ingest/import-bib` that accepts `{entries: [{citekey, ...}]}` (the selected subset) and ingests them, copying the BibTeX entries to `bib/srcbib/`. Use background action pattern with progress.
> 3. **Frontend** — add a drag-and-drop zone in ActionsTab (or a dedicated "Import" section):
>    - Drop a `.bib` file or click to upload
>    - Show preview table: checkbox, citekey, title, authors, year, DOI, status (new/duplicate)
>    - Duplicates are dimmed and unchecked by default
>    - "Select all new" / "Select none" controls
>    - "Import Selected" button → calls the import endpoint with checked entries
>    - Progress bar during import
> 4. Also support pasting raw BibTeX text into a textarea as an alternative to file upload
> 5. **Tests** — test preview parsing and duplicate detection
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add batch BibTeX import with GUI preview and drag-and-drop".

## Acceptance Criteria

- [ ] 

## Result

Completed in 645.4s (1 steps, model: opus)

Session: `17be28cd-d6da-4b88-bdbb-d17522a1dbb1`
Result note: `/storage2/arash/worklog/workflow/captures/20260329-165727-997110/note.md`

### Output

```
All done. Here's a summary of what was implemented:

**Backend** (`src/biblio/_pybtex_utils.py`):
- Added `parse_bibtex_string()` for parsing BibTeX from text strings

**Backend** (`src/biblio/ingest.py`):
- `preview_bibtex(bibtex_text, repo_root)` — parses BibTeX text, returns `BibPreviewEntry` list with duplicate detection (checks both existing DOIs and citekeys in srcbib + citekeys.md)
- `import_bibtex_entries(bibtex_text, selected_citekeys, repo_root)` — imports selected entries to `srcbib/imported.bib`, registers in `citekeys.md`, logs to `imports.jsonl`

**API** (`src/biblio/ui.py`):
- `POST /api/ingest/preview-bib` — accepts multipart file upload or `bibtex_text` form field, returns parsed entries with `already_exists` flags
- `POST /api/ingest/import-bib` — accepts `{bibtex_text, entries}`, runs import in background thread
- `GET /api/ingest/import-bib/status` — poll import progress

**Frontend** (`frontend/src/components/ActionsTab.jsx`):
- `BibImportPanel` with drag-and-drop zone for `.bib` files
- Click-to-browse file picker
- "Paste BibTeX text" toggle with textarea
- Preview table with checkboxes, citekey, title, authors, year, status (new/duplicate)
- Duplicates dimmed at 45% opacity and unchecked by default
- "Select all new" / "Select none" controls
- "Import Selected" button with progress polling
- Import result display with "Import More" reset

**Tests** (`tests/test_biblio_ingest.py`):
- 7 new tests covering preview parsing, DOI duplicate detection, citekey duplicate detection, selective import, full import, and empty selection handling

171 existing tests pass, frontend builds clean.

## Next Steps
1. Run `biblio ui serve` and test the drag-and-drop BibTeX import with a real `.bib` file to verify the end-to-end flow.
2. Add the ingest test file (`test_biblio_ingest.py`) to the default `make test` target so it runs in CI.
3. Add a "Run Merge BibTeX" auto-prompt after import completes, so newly imported entries get merged into the main bibliography
```

