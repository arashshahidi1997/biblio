---
title: "GUI: Tag vocabulary autocomplete and library lint panel"
date: 2026-03-28
timestamp: 20260328-210153-600539
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260328-211931-474f4a/note.md
completed: 2026-03-28T21:19:32+01:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260328-205727-396172.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# GUI: Tag vocabulary autocomplete and library lint panel

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the following frontend files to understand the current UI architecture:
> - `frontend/src/App.jsx` — main app state, tab switching
> - `frontend/src/components/PaperTab.jsx` — especially the `LibraryPanel` component (tag input around lines 24-69) and the `ExploreTab.jsx` tag filter
> - `frontend/src/components/ActionsTab.jsx` — action buttons layout
> - `frontend/src/App.css` — styling conventions
>
> Also read `src/biblio/ui.py` to understand the API endpoints for tag vocab and lint (search for `/api/tag-vocab` and `/api/library/lint`).
>
> Implement the following frontend changes:
>
> ### 1. Tag autocomplete component
> Create a reusable `TagInput` component (can be inline in PaperTab.jsx or a new file in `frontend/src/components/`):
> - Fetch vocabulary from `GET /api/tag-vocab` on mount (cache it)
> - As user types, show a dropdown overlay with matching namespace:value suggestions
> - Typing `domain:` should show all domain values; typing `meth` should show `method:` namespace and matching values
> - Clicking a suggestion or pressing Tab/Enter inserts the tag
> - Show existing tags as removable pills/chips above the input
> - Support unnamespaced free-form tags too (no enforcement)
> - Style the dropdown to match the app's dark theme (use CSS variables from App.css)
>
> ### 2. Replace tag input in LibraryPanel
> Replace the plain `<input>` tag field in LibraryPanel with the new TagInput component. Wire up save to the existing `updateLibraryEntry` function.
>
> ### 3. Tag filter autocomplete in ExploreTab
> The ExploreTab has a `tagFilter` state for filtering the paper list. Add the same autocomplete behavior to this filter input.
>
> ### 4. Library lint panel in ActionsTab
> Add a "Tag Health" section to ActionsTab:
> - Button: "Lint Tags" that calls `POST /api/library/lint`
> - Display results: list of issues grouped by type (non-vocabulary, duplicates, inconsistent casing)
> - Each issue shows the affected citekey(s) and suggested fix
> - "Apply suggestion" button per issue that calls `POST /api/library/{citekey}` to update tags
>
> ### 5. Auto-tag action button
> Add "Auto-tag Selected" and "Auto-tag All Untagged" buttons to ActionsTab that call `POST /api/actions/autotag` with appropriate body. Show status via the existing `actionState` pattern.
>
> Follow existing component patterns and CSS conventions. After all changes, run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add tag autocomplete and library lint panel to GUI".

---
Source: docs/log/task/task-arash-20260328-205727-396172.md

# GUI: Tag vocabulary autocomplete and library lint panel

## Goal

Upgrade the tag input in PaperTab's LibraryPanel to support vocabulary-aware autocomplete with faceted namespaces, and add a lint panel to the ActionsTab that shows tag inconsistencies.

## Context

- Current tag input in PaperTab `LibraryPanel` (line ~28-69 of PaperTab.jsx) is a plain text `<input>` with comma-separated tags
- Backend exposes `GET /api/tag-vocab` (vocabulary) and `POST /api/library/lint` (lint report) — from task 1
- Tag vocabulary has namespaces (`domain:`, `method:`, `task:`, `corpus:`) with suggested values
- ExploreTab has `tagFilter` state — should also benefit from autocomplete
- No external autocomplete library needed — a simple dropdown overlay works

## Prompt

> Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the following frontend files to understand the current UI architecture:
> - `frontend/src/App.jsx` — main app state, tab switching
> - `frontend/src/components/PaperTab.jsx` — especially the `LibraryPanel` component (tag input around lines 24-69) and the `ExploreTab.jsx` tag filter
> - `frontend/src/components/ActionsTab.jsx` — action buttons layout
> - `frontend/src/App.css` — styling conventions
>
> Also read `src/biblio/ui.py` to understand the API endpoints for tag vocab and lint (search for `/api/tag-vocab` and `/api/library/lint`).
>
> Implement the following frontend changes:
>
> ### 1. Tag autocomplete component
> Create a reusable `TagInput` component (can be inline in PaperTab.jsx or a new file in `frontend/src/components/`):
> - Fetch vocabulary from `GET /api/tag-vocab` on mount (cache it)
> - As user types, show a dropdown overlay with matching namespace:value suggestions
> - Typing `domain:` should show all domain values; typing `meth` should show `method:` namespace and matching values
> - Clicking a suggestion or pressing Tab/Enter inserts the tag
> - Show existing tags as removable pills/chips above the input
> - Support unnamespaced free-form tags too (no enforcement)
> - Style the dropdown to match the app's dark theme (use CSS variables from App.css)
>
> ### 2. Replace tag input in LibraryPanel
> Replace the plain `<input>` tag field in LibraryPanel with the new TagInput component. Wire up save to the existing `updateLibraryEntry` function.
>
> ### 3. Tag filter autocomplete in ExploreTab
> The ExploreTab has a `tagFilter` state for filtering the paper list. Add the same autocomplete behavior to this filter input.
>
> ### 4. Library lint panel in ActionsTab
> Add a "Tag Health" section to ActionsTab:
> - Button: "Lint Tags" that calls `POST /api/library/lint`
> - Display results: list of issues grouped by type (non-vocabulary, duplicates, inconsistent casing)
> - Each issue shows the affected citekey(s) and suggested fix
> - "Apply suggestion" button per issue that calls `POST /api/library/{citekey}` to update tags
>
> ### 5. Auto-tag action button
> Add "Auto-tag Selected" and "Auto-tag All Untagged" buttons to ActionsTab that call `POST /api/actions/autotag` with appropriate body. Show status via the existing `actionState` pattern.
>
> Follow existing component patterns and CSS conventions. After all changes, run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add tag autocomplete and library lint panel to GUI".

## Acceptance Criteria

- [ ] TagInput component with vocabulary-aware autocomplete dropdown
- [ ] Tag pills/chips display with remove buttons
- [ ] LibraryPanel uses TagInput instead of plain text input
- [ ] ExploreTab tag filter has autocomplete
- [ ] ActionsTab shows "Tag Health" lint section with per-issue suggestions
- [ ] Auto-tag action buttons in ActionsTab
- [ ] Styling matches existing dark theme
- [ ] Changes committed via `datalad_save`

## Result

(Filled in after execution)

## Acceptance Criteria

- [ ] 

## Result

Completed in 282.2s (1 steps, model: opus)

Session: `a3c281ce-d9e9-4b85-b099-7a7fda14468f`
Result note: `/storage2/arash/worklog/workflow/captures/20260328-211931-474f4a/note.md`

### Output

```
All done. Here's a summary of what was implemented:

**1. TagInput component** (`frontend/src/components/TagInput.jsx`):
- Fetches vocabulary from `GET /api/tag-vocab` on mount with caching
- Dropdown overlay with matching `namespace:value` suggestions (typing `domain:` shows all domain values, partial matches work too)
- Tab/Enter inserts suggestion, click works, arrow keys navigate
- Tags shown as removable pills/chips
- Supports free-form tags (no enforcement)
- `mode="filter"` for single-select behavior

**2. LibraryPanel** — replaced plain `<input>` with `TagInput`, wired to existing `updateLibraryEntry`

**3. CorpusTab tag filter** — replaced `<select>` dropdown with `TagInput` in filter mode (single-select with autocomplete)

**4. ActionsTab lint panel** — "Tag Health" section with "Lint Tags" button calling `POST /api/library/lint`, results grouped by type (non-vocabulary, duplicates, suggestions), "Apply" button per suggestion

**5. Auto-tag buttons** — "Auto-tag Selected" and "Auto-tag All Untagged" buttons using the existing `autotag` action with async polling support

**Tests**: 130 passed. Saved with datalad.

## Next Steps
1. Wire `ActionsTab` into the main layout (it's currently imported but not rendered — add it as a sidebar panel or new tab in `App.jsx`).
2. Add keyboard shortcut support (e.g., comma to confirm a tag) and multi-select mode for the tag filter in CorpusTab.
3. Add a "Fix All" bulk action to the lint panel that applies all safe suggestions in one click.
```

