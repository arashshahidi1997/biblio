---
title: "Smart collections via saved query filters"
date: 2026-03-28
timestamp: 20260328-183530-864527
status: scheduled
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260328-183236-620818.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Smart collections via saved query filters

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the source idea note referenced in Context above. Read `src/biblio/collections.py`, `src/biblio/library.py`, `src/biblio/cli.py`, and `src/biblio/mcp.py` to understand the existing collections and library systems.
>
> Implement the following:
>
> 1. **Query DSL** — Design a minimal boolean query language supporting: field predicates (`tag:method:transformer`, `status:unread`, `priority:high`, `author:Smith`, `year:2024`, `year:>2022`), boolean operators (`AND`, `OR`, `NOT`), and parenthetical grouping. Implement a parser and evaluator in a new `query.py` module.
>
> 2. **Smart collection schema** — Extend the collections data model: if a collection has a `query` field it's smart (dynamic membership); if it has `citekeys` it's manual. Both types keep `name` and optional `description`.
>
> 3. **Query evaluator** — Implement evaluation against library.yml entries + BibTeX metadata. The evaluator loads both data sources and filters citekeys matching the boolean expression. Must be fast (in-memory, library.yml is small).
>
> 4. **CLI commands** — Add `biblio collection create --smart "<query>" "<name>"` to create smart collections. `biblio collection list` should show both types with membership counts. `biblio collection show <name>` should resolve smart collection membership dynamically.
>
> 5. **MCP tools** — Extend collection MCP tools to support smart collection creation, listing, and query editing.
>
> 6. **Tests** — Test the query parser, evaluator (with various predicate combinations), smart collection creation, and dynamic resolution.
>
> After all changes, run tests with `make test`. Then use `datalad_save` MCP tool to commit with message: "Add smart collections with boolean query filters".

---
Source: docs/log/task/task-arash-20260328-183236-620818.md

# Smart collections via saved query filters

## Goal

Add dynamic "smart collections" defined by boolean query filters over tags, status, priority, author, and year — coexisting with existing manual collections.

## Context

- Source idea: `idea-arash-20260328-182639-838518`
- Existing `src/biblio/collections.py` handles manual collections (static citekey lists)
- Depends on task 1 (tag vocabulary) for faceted tag queries to be useful
- No AI/LLM dependency — pure deterministic query evaluation
- Query evaluation runs against library.yml + BibTeX metadata in-memory

## Prompt

> Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the source idea note referenced in Context above. Read `src/biblio/collections.py`, `src/biblio/library.py`, `src/biblio/cli.py`, and `src/biblio/mcp.py` to understand the existing collections and library systems.
>
> Implement the following:
>
> 1. **Query DSL** — Design a minimal boolean query language supporting: field predicates (`tag:method:transformer`, `status:unread`, `priority:high`, `author:Smith`, `year:2024`, `year:>2022`), boolean operators (`AND`, `OR`, `NOT`), and parenthetical grouping. Implement a parser and evaluator in a new `query.py` module.
>
> 2. **Smart collection schema** — Extend the collections data model: if a collection has a `query` field it's smart (dynamic membership); if it has `citekeys` it's manual. Both types keep `name` and optional `description`.
>
> 3. **Query evaluator** — Implement evaluation against library.yml entries + BibTeX metadata. The evaluator loads both data sources and filters citekeys matching the boolean expression. Must be fast (in-memory, library.yml is small).
>
> 4. **CLI commands** — Add `biblio collection create --smart "<query>" "<name>"` to create smart collections. `biblio collection list` should show both types with membership counts. `biblio collection show <name>` should resolve smart collection membership dynamically.
>
> 5. **MCP tools** — Extend collection MCP tools to support smart collection creation, listing, and query editing.
>
> 6. **Tests** — Test the query parser, evaluator (with various predicate combinations), smart collection creation, and dynamic resolution.
>
> After all changes, run tests with `make test`. Then use `datalad_save` MCP tool to commit with message: "Add smart collections with boolean query filters".

## Acceptance Criteria

- [ ] Query DSL parser handles `tag:`, `status:`, `priority:`, `author:`, `year:` predicates with AND/OR/NOT
- [ ] Collections schema supports both smart (query) and manual (citekeys) types
- [ ] Smart collection membership resolves dynamically at display time
- [ ] CLI supports `--smart` flag for collection creation
- [ ] MCP tools updated for smart collections
- [ ] Tests pass for parser, evaluator, and collection CRUD
- [ ] Changes committed via `datalad_save`

## Result

(Filled in after execution)

## Acceptance Criteria

- [ ] 

## Result

(pending)

