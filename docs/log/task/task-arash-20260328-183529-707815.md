---
title: "Controlled tag vocabulary and BibTeX keyword sync"
date: 2026-03-28
timestamp: 20260328-183529-707815
status: scheduled
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260328-183236-153457.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Controlled tag vocabulary and BibTeX keyword sync

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the source idea notes referenced in Context above. Read `src/biblio/library.py`, `src/biblio/ingest.py`, `src/biblio/cli.py`, `src/biblio/mcp.py`, and `src/biblio/config.py` to understand existing code.
>
> Implement the following:
>
> 1. **Tag vocabulary file** — Create schema and default `bib/config/tag_vocab.yml` with initial namespaces (`domain`, `method`, `task`, `corpus`). Include a few seed values per namespace. The scaffold should create this file on `biblio init`.
>
> 2. **Tag validation utility** — Add a `tag_vocab.py` module with functions to: load vocabulary, validate a tag against it (return whether it matches a known namespace:value), normalize tags (lowercase, strip whitespace), and suggest corrections for close misses.
>
> 3. **BibTeX keyword sync** — In `ingest.py`, after successful DOI/entry resolution, extract the BibTeX `keywords` field, normalize them, map to vocabulary namespaces where possible (e.g. keyword "NLP" → `domain:nlp`), and write them to library.yml tags for that citekey. Unrecognized keywords should still be added as unnamespaced tags.
>
> 4. **CLI lint command** — Add `biblio library lint` subcommand that scans all library.yml tags, reports non-vocabulary tags, duplicate/inconsistent tags (e.g. "NLP" vs "nlp"), and suggests namespace mappings.
>
> 5. **MCP exposure** — Add a `biblio_tag_vocab()` MCP tool that returns the current vocabulary for agent use.
>
> 6. **Tests** — Add tests for tag normalization, vocabulary validation, keyword extraction from BibTeX entries, and the lint command.
>
> After all changes, run tests with `make test`. Then use `datalad_save` MCP tool to commit with message: "Add controlled tag vocabulary and BibTeX keyword sync".

---
Source: docs/log/task/task-arash-20260328-183236-153457.md

# Controlled tag vocabulary and BibTeX keyword sync

## Goal

Establish a faceted tag namespace convention (`domain:`, `method:`, `task:`, `corpus:`) with a vocabulary file for autocomplete/validation, and implement BibTeX keyword extraction on ingest as the first source of tags.

## Context

- Source ideas: `idea-arash-20260328-182638-621544` (vocabulary) and Tier 1 of `idea-arash-20260328-182639-622041` (keyword sync)
- Tags in `bib/config/library.yml` are currently free-form strings — no structure or validation
- BibTeX entries often have a `keywords` field from publishers — free signal that goes unused
- This is the foundation for auto-tagging (task 3) and smart collections (task 2)
- No AI/LLM dependency — pure deterministic tooling

## Prompt

> Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the source idea notes referenced in Context above. Read `src/biblio/library.py`, `src/biblio/ingest.py`, `src/biblio/cli.py`, `src/biblio/mcp.py`, and `src/biblio/config.py` to understand existing code.
>
> Implement the following:
>
> 1. **Tag vocabulary file** — Create schema and default `bib/config/tag_vocab.yml` with initial namespaces (`domain`, `method`, `task`, `corpus`). Include a few seed values per namespace. The scaffold should create this file on `biblio init`.
>
> 2. **Tag validation utility** — Add a `tag_vocab.py` module with functions to: load vocabulary, validate a tag against it (return whether it matches a known namespace:value), normalize tags (lowercase, strip whitespace), and suggest corrections for close misses.
>
> 3. **BibTeX keyword sync** — In `ingest.py`, after successful DOI/entry resolution, extract the BibTeX `keywords` field, normalize them, map to vocabulary namespaces where possible (e.g. keyword "NLP" → `domain:nlp`), and write them to library.yml tags for that citekey. Unrecognized keywords should still be added as unnamespaced tags.
>
> 4. **CLI lint command** — Add `biblio library lint` subcommand that scans all library.yml tags, reports non-vocabulary tags, duplicate/inconsistent tags (e.g. "NLP" vs "nlp"), and suggests namespace mappings.
>
> 5. **MCP exposure** — Add a `biblio_tag_vocab()` MCP tool that returns the current vocabulary for agent use.
>
> 6. **Tests** — Add tests for tag normalization, vocabulary validation, keyword extraction from BibTeX entries, and the lint command.
>
> After all changes, run tests with `make test`. Then use `datalad_save` MCP tool to commit with message: "Add controlled tag vocabulary and BibTeX keyword sync".

## Acceptance Criteria

- [ ] `bib/config/tag_vocab.yml` schema defined with initial namespaces and seed values
- [ ] `biblio init` scaffold creates the vocabulary file
- [ ] BibTeX `keywords` field extracted and mapped to tags on ingest
- [ ] `biblio library lint` reports non-vocabulary and inconsistent tags
- [ ] `biblio_tag_vocab` MCP tool returns current vocabulary
- [ ] Tests pass for normalization, validation, keyword extraction
- [ ] Changes committed via `datalad_save`

## Result

(Filled in after execution)

## Acceptance Criteria

- [ ] 

## Result

(pending)

