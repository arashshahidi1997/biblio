---
title: "Entry type classification, flagging non-papers, and filtered views"
date: 2026-03-29
timestamp: 20260329-042756-903726
status: scheduled
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-042714-899540.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Entry type classification, flagging non-papers, and filtered views

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/bibtex.py` and `src/biblio/_pybtex_utils.py` to understand how BibTeX entries are parsed and what `entry.type` contains. Read `src/biblio/ui.py` — the `/api/model` endpoint to see what per-paper data is returned, and the batch action endpoints (docling-run all, grobid-run all, fetch-pdfs-oa, autotag). Read `frontend/src/components/ExploreTab.jsx` for filtering. Read `src/biblio/query.py` for the existing `type:` predicate.
>
> Implement:
>
> ### 1. Entry type classification
> Add to the model data for each paper: `entry_type` (raw BibTeX type) and `is_paper` (boolean).
> Classification:
> - **Paper types** (is_paper=true): `article`, `inproceedings`, `conference`, `incollection`, `unpublished`
> - **Non-paper types** (is_paper=false): `book`, `proceedings`, `phdthesis`, `mastersthesis`, `techreport`, `misc`, `manual`, `booklet`, `online`
> - `@misc` is ambiguous — if it has a DOI, treat as paper; otherwise non-paper
> - Store classification in the model data returned by `/api/model`
>
> ### 2. Library view filtering
> In ExploreTab:
> - Add a toggle/filter: "Papers only" (default ON) / "All entries"
> - When "Papers only" is on, hide non-paper entries from the list
> - Non-paper entries should have a distinct visual indicator when shown (e.g. a small type badge: "book", "thesis", "dataset")
> - The entry type badge should be shown in the paper row alongside other metadata
>
> ### 3. Batch action filtering
> Modify all batch action backends to respect the paper filter:
> - `docling-run {all: true}` — only process papers with PDFs
> - `grobid-run {all: true}` — only process papers
> - `fetch-pdfs-oa` — only fetch for papers (books/theses unlikely to have OA PDFs)
> - `autotag {all_untagged: true}` — only tag papers
> - `summarize {status: "unread"}` — only summarize papers
> - Add an optional `include_non_papers: true` flag to override the default filtering
> - The GUI batch action buttons should mention the count: "Run Docling For All (42 papers)" not including non-papers
>
> ### 4. Stats bar integration
> The stats bar (from the batch docling task) should show: `542 papers · 135 other entries · 207 pdf · ...`
> Smart tips should only count papers for suggestions.
>
> ### 5. PaperTab handling
> Non-paper entries can still be opened in PaperTab but should show the entry type prominently: "This is a book entry" with adapted UI (e.g. don't show "Generate Summary" for a book unless the user wants to).
>
> ### 6. Smart collection support
> The `type:` predicate already exists in query.py. Ensure it works with the new classification:
> - `type:article` matches articles
> - `type:paper` matches all paper types (convenience alias)
> - `type:book`, `type:thesis`, etc.
> - Smart collection example: `type:paper AND status:unread`
>
> After all changes, run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add entry type classification, non-paper flagging, and filtered views".

---
Source: docs/log/task/task-arash-20260329-042714-899540.md

# Entry type classification, flagging non-papers, and filtered views

## Goal

Classify BibTeX entries by type (article, book, thesis, dataset, software, etc.), flag non-paper entries, and filter them out by default in library views and batch actions so they don't clog paper-oriented workflows.

## Context

- BibTeX sources contain mixed entry types: `@article`, `@inproceedings`, `@book`, `@phdthesis`, `@mastersthesis`, `@misc`, `@techreport`, `@proceedings`, `@incollection`, `@manual`, `@unpublished`, etc.
- Only some are "papers": `@article`, `@inproceedings`, `@conference`, `@incollection`, `@unpublished` (preprints)
- Others are non-paper references: `@book`, `@proceedings` (the volume, not a paper), `@phdthesis`, `@mastersthesis`, `@techreport`, `@misc` (could be datasets, software, web pages), `@manual`
- Batch actions like "Run Docling For All", "Fetch OA PDFs", "Auto-tag All" should only target papers by default
- ExploreTab should default to showing papers, with an option to include non-papers
- The entry type is already in the BibTeX data — just needs to be surfaced and used for filtering
- Smart collections query DSL already has `type:` predicate (from the query.py module) — this task makes it first-class

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/bibtex.py` and `src/biblio/_pybtex_utils.py` to understand how BibTeX entries are parsed and what `entry.type` contains. Read `src/biblio/ui.py` — the `/api/model` endpoint to see what per-paper data is returned, and the batch action endpoints (docling-run all, grobid-run all, fetch-pdfs-oa, autotag). Read `frontend/src/components/ExploreTab.jsx` for filtering. Read `src/biblio/query.py` for the existing `type:` predicate.
>
> Implement:
>
> ### 1. Entry type classification
> Add to the model data for each paper: `entry_type` (raw BibTeX type) and `is_paper` (boolean).
> Classification:
> - **Paper types** (is_paper=true): `article`, `inproceedings`, `conference`, `incollection`, `unpublished`
> - **Non-paper types** (is_paper=false): `book`, `proceedings`, `phdthesis`, `mastersthesis`, `techreport`, `misc`, `manual`, `booklet`, `online`
> - `@misc` is ambiguous — if it has a DOI, treat as paper; otherwise non-paper
> - Store classification in the model data returned by `/api/model`
>
> ### 2. Library view filtering
> In ExploreTab:
> - Add a toggle/filter: "Papers only" (default ON) / "All entries"
> - When "Papers only" is on, hide non-paper entries from the list
> - Non-paper entries should have a distinct visual indicator when shown (e.g. a small type badge: "book", "thesis", "dataset")
> - The entry type badge should be shown in the paper row alongside other metadata
>
> ### 3. Batch action filtering
> Modify all batch action backends to respect the paper filter:
> - `docling-run {all: true}` — only process papers with PDFs
> - `grobid-run {all: true}` — only process papers
> - `fetch-pdfs-oa` — only fetch for papers (books/theses unlikely to have OA PDFs)
> - `autotag {all_untagged: true}` — only tag papers
> - `summarize {status: "unread"}` — only summarize papers
> - Add an optional `include_non_papers: true` flag to override the default filtering
> - The GUI batch action buttons should mention the count: "Run Docling For All (42 papers)" not including non-papers
>
> ### 4. Stats bar integration
> The stats bar (from the batch docling task) should show: `542 papers · 135 other entries · 207 pdf · ...`
> Smart tips should only count papers for suggestions.
>
> ### 5. PaperTab handling
> Non-paper entries can still be opened in PaperTab but should show the entry type prominently: "This is a book entry" with adapted UI (e.g. don't show "Generate Summary" for a book unless the user wants to).
>
> ### 6. Smart collection support
> The `type:` predicate already exists in query.py. Ensure it works with the new classification:
> - `type:article` matches articles
> - `type:paper` matches all paper types (convenience alias)
> - `type:book`, `type:thesis`, etc.
> - Smart collection example: `type:paper AND status:unread`
>
> After all changes, run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add entry type classification, non-paper flagging, and filtered views".

## Acceptance Criteria

- [ ] 

## Result

(pending)

