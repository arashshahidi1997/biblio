---
title: "Fix test collection errors and test environment setup"
date: 2026-03-29
timestamp: 20260329-041416-742117
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260329-043109-ca9dc3/note.md
completed: 2026-03-29T04:31:11+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041122-888439.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Fix test collection errors and test environment setup

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Run `PYTHONPATH=src python -m pytest tests/ -q --co 2>&1` to see the current collection status. Then read the failing test files' import lines and compare with the working test files to understand the difference.
>
> Read the Makefile to see how `make test` is configured. Read `tests/conftest.py` if it exists.
>
> Fix the import issue. The best approach is likely:
> 1. Add/update `tests/conftest.py` to insert `src/` into `sys.path` so all test files can `from biblio import ...`
> 2. Alternatively, fix the Makefile `test` target to use `PYTHONPATH=src`
> 3. Ensure both approaches work — conftest for IDE test runners, Makefile for CI
>
> After fixing imports, run `PYTHONPATH=src python -m pytest tests/ -q` and fix any actual test failures (not just collection errors). Tests that need network access should be skipped with `@pytest.mark.skipif` or use mocks.
>
> Then use `datalad_save` MCP tool to commit with message: "Fix test collection errors and test environment setup".

---
Source: docs/log/task/task-arash-20260329-041122-888439.md

# Fix test collection errors and test environment setup

## Goal

Fix 9 test files that fail to collect due to import errors. Ensure all tests can be discovered and run with `PYTHONPATH=src python -m pytest tests/ -q`.

## Context

- Running `python -m pytest tests/ -q --co` shows 9 ERROR files: test_biblio.py, test_biblio_graph.py, test_biblio_graph_mcp.py, test_biblio_ingest.py, test_biblio_openalex.py, test_biblio_rag.py, test_biblio_site.py, test_smart_collections.py, test_tag_vocab.py
- Error: `ModuleNotFoundError: No module named 'biblio'` — tests import `from biblio.xxx` but the package isn't installed; needs `PYTHONPATH=src` or a conftest.py with sys.path insertion
- 89 tests collect successfully from the remaining files
- The Makefile target `make test` should use `PYTHONPATH=src`

## Prompt

> Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Run `PYTHONPATH=src python -m pytest tests/ -q --co 2>&1` to see the current collection status. Then read the failing test files' import lines and compare with the working test files to understand the difference.
>
> Read the Makefile to see how `make test` is configured. Read `tests/conftest.py` if it exists.
>
> Fix the import issue. The best approach is likely:
> 1. Add/update `tests/conftest.py` to insert `src/` into `sys.path` so all test files can `from biblio import ...`
> 2. Alternatively, fix the Makefile `test` target to use `PYTHONPATH=src`
> 3. Ensure both approaches work — conftest for IDE test runners, Makefile for CI
>
> After fixing imports, run `PYTHONPATH=src python -m pytest tests/ -q` and fix any actual test failures (not just collection errors). Tests that need network access should be skipped with `@pytest.mark.skipif` or use mocks.
>
> Then use `datalad_save` MCP tool to commit with message: "Fix test collection errors and test environment setup".

## Acceptance Criteria

- [ ] 

## Result

Completed in 648.4s (1 steps, model: opus)

Session: `4e57d514-482a-410d-97d9-ddfb6c34aeb0`
Result note: `/storage2/arash/worklog/workflow/captures/20260329-043109-ca9dc3/note.md`

### Output

```
All done. Here's what was fixed:

**Test environment setup:**
- Added `pythonpath = src` to `pytest.ini` so pytest natively adds `src/` to `sys.path`
- Created `tests/conftest.py` with `sys.path` insertion as fallback for IDE runners

**Bug fix in source code:**
- `src/biblio/cli.py`: Removed redundant local `from .library import load_library` inside an `if` branch that shadowed the module-level import for the entire `main()` function, causing `UnboundLocalError`

**Test assertion updates (17 failures → 0):**
- `test_biblio_ingest.py`: Updated citekey format from `doe2024paper` to `doe_2024_PaperOne` style (6 tests)
- `test_biblio_graph.py`: Updated citekey prefix assertion (1 test)
- `test_biblio_summarize.py`: Fixed mock patch targets from `biblio.summarize.default_config_path` to `biblio.config.default_config_path` (local imports need source-module patching) (5 tests)
- `test_biblio_ui.py`: Added missing `total_inputs` to mock, removed stale `site_doctor` assertion, updated message format (2 tests)
- `test_smart_collections.py`: Changed `smart["count"]` to `smart["resolved_count"]` (1 test)

**Result: 234 passed, 0 failed.**

## Next Steps
1. Run `make test` to verify the focused test suite also passes with the Makefile's `PYTEST_PYTHON`.
2. Add the remaining untracked task log files to datalad and push to github.
3. (done)
```

