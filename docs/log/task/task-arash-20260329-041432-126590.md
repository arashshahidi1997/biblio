---
title: "Library statistics dashboard panel"
date: 2026-03-29
timestamp: 20260329-041432-126590
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260329-052606-7694f7/note.md
completed: 2026-03-29T05:26:07+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041126-533404.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Library statistics dashboard panel

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/App.jsx` and `frontend/src/components/ExploreTab.jsx` to understand where stats could fit. Read `src/biblio/ui.py` for the `/api/model` endpoint to see what data is already available.
>
> Implement:
> 1. **Backend** — add `GET /api/stats` endpoint that computes:
>    - Total papers count
>    - Papers by status (unread/reading/processed/archived/unset)
>    - Papers by year (histogram data)
>    - Top 15 tags by frequency
>    - Tag namespace distribution (how many domain:, method:, task: tags)
>    - Derivative coverage: % with PDF, % with docling, % with grobid, % with summary, % with concepts
>    - Papers missing PDF count
>    - Papers in fetch queue count
> 2. **Frontend** — add a stats panel. Options for placement:
>    - A collapsible panel at the top of ExploreTab (preferred — visible when browsing)
>    - Or a dedicated "Dashboard" subtab in the library view
>    - Show: status donut chart (CSS-only, no chart library), year histogram (CSS bar chart), tag cloud, derivative coverage bars
>    - Each stat should be clickable to filter the paper list (e.g. click "unread: 42" filters to unread)
> 3. **CSS** — minimal chart styling using CSS variables, no external chart library needed. Use colored bars/segments.
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add library statistics dashboard panel".

---
Source: docs/log/task/task-arash-20260329-041126-533404.md

# Library statistics dashboard panel

## Goal

Add a statistics overview panel to the GUI showing library health: papers by status, year distribution, tag coverage, derivative completeness.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/App.jsx` and `frontend/src/components/ExploreTab.jsx` to understand where stats could fit. Read `src/biblio/ui.py` for the `/api/model` endpoint to see what data is already available.
>
> Implement:
> 1. **Backend** — add `GET /api/stats` endpoint that computes:
>    - Total papers count
>    - Papers by status (unread/reading/processed/archived/unset)
>    - Papers by year (histogram data)
>    - Top 15 tags by frequency
>    - Tag namespace distribution (how many domain:, method:, task: tags)
>    - Derivative coverage: % with PDF, % with docling, % with grobid, % with summary, % with concepts
>    - Papers missing PDF count
>    - Papers in fetch queue count
> 2. **Frontend** — add a stats panel. Options for placement:
>    - A collapsible panel at the top of ExploreTab (preferred — visible when browsing)
>    - Or a dedicated "Dashboard" subtab in the library view
>    - Show: status donut chart (CSS-only, no chart library), year histogram (CSS bar chart), tag cloud, derivative coverage bars
>    - Each stat should be clickable to filter the paper list (e.g. click "unread: 42" filters to unread)
> 3. **CSS** — minimal chart styling using CSS variables, no external chart library needed. Use colored bars/segments.
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add library statistics dashboard panel".

## Acceptance Criteria

- [ ] 

## Result

Completed in 268.7s (1 steps, model: opus)

Session: `e78966df-2cb7-431f-8760-43ff189be027`
Result note: `/storage2/arash/worklog/workflow/captures/20260329-052606-7694f7/note.md`

### Output

```
Done. All 171 tests pass, changes saved.

**What was implemented:**

**Backend** (`src/biblio/ui.py`) — `GET /api/stats` endpoint returning:
- Total paper count, papers by status, papers by year (histogram data)
- Top 15 tags by frequency, tag namespace distribution
- Derivative coverage percentages (PDF, docling, grobid, summary, concepts)
- Missing PDF count, fetch queue count

**Frontend** (`frontend/src/components/StatsPanel.jsx`) — Collapsible panel at the top of the library list column with:
- SVG donut chart for status distribution (clickable to filter)
- CSS bar chart for year histogram
- Coverage progress bars for each derivative type
- Tag cloud (clickable to filter paper list)
- Tag namespace breakdown chips

**CSS** (`frontend/src/App.css`) — All chart styling uses CSS variables (`--accent`, `--line`, `--muted`, etc.), no external chart library. Responsive grid layout for stats sections.

**Integration** (`frontend/src/App.jsx`) — StatsPanel placed above CorpusTab in library view. Status clicks set `statusFilter`, tag clicks set `tagFilter`, both filtering the paper list.

## Next Steps
1. Add a backend test for the `/api/stats` endpoint to verify the response schema and counts.
2. Add a "refresh stats" button or auto-refresh stats when the model reloads after actions complete.
3. Make year histogram bars clickable to filter papers by year range.
```

