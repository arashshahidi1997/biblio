---
title: "Paper summary pipeline"
date: 2026-03-28
timestamp: 20260328-183533-140894
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260328-185916-989e5c/note.md
completed: 2026-03-28T18:59:18+01:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260328-183238-162741.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Paper summary pipeline

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the source idea note referenced in Context above. Read `src/biblio/docling.py` and `src/biblio/grobid.py` to understand the existing derivative pattern. Read `src/biblio/mcp.py` (especially the `paper_context` function), `src/biblio/cli.py`, `src/biblio/config.py`, and `src/biblio/site.py` (to see how it scans for derivatives).
>
> Implement the following:
>
> 1. **Summary module** — Create `src/biblio/summarize.py` with:
>    - `assemble_context(citekey, root)`: gathers paper_context (BibTeX metadata, docling markdown, GROBID header/references) into a structured prompt input. Returns the assembled text.
>    - `summarize(citekey, root, prompt_only=False)`: assembles context, then either returns the prompt (if `prompt_only=True`) or calls LLM to generate a structured summary. Uses Anthropic Python SDK. Skip gracefully if no API key.
>    - Summary output sections: **Contribution**, **Method**, **Key Findings**, **Limitations**, **Relevance** (to project context).
>
> 2. **Summary template** — Define the markdown template for `bib/derivatives/summaries/{citekey}.md` with YAML frontmatter (citekey, date_generated, model_used) and the structured sections.
>
> 3. **CLI command** — Add `biblio summarize <citekey> [--prompt-only] [--status unread] [--force]`. `--prompt-only` prints the assembled prompt to stdout (pipe-friendly). `--status` enables batch mode over papers with that status. `--force` regenerates existing summaries.
>
> 4. **MCP tool** — Add `biblio_summarize(citekey, prompt_only=False)` MCP tool.
>
> 5. **Site integration** — Verify that `site.py` already picks up files from `bib/derivatives/summaries/`. If not, add the glob pattern.
>
> 6. **Tests** — Test context assembly (mock paper_context), summary template generation, `--prompt-only` mode, and batch filtering.
>
> After all changes, run tests with `make test`. Then use `datalad_save` MCP tool to commit with message: "Add paper summary pipeline with prompt-only mode".

---
Source: docs/log/task/task-arash-20260328-183238-162741.md

# Paper summary pipeline

## Goal

Implement LLM-generated structured paper summaries stored as `bib/derivatives/summaries/{citekey}.md`. Includes CLI command, MCP tool, batch mode, and a `--prompt-only` mode for non-LLM use.

## Context

- Source idea: `idea-arash-20260328-182615-773227`
- Fits the existing derivative pattern (like docling, grobid outputs)
- Site builder already scans for `*summary*` files — should work with zero integration
- Prerequisite for: comparison tables, reading list curation, lit reviews, presentations
- Should support `--prompt-only` to emit assembled context without calling LLM

## Prompt

> Call `project_context()` and `runtime_conventions()` to understand the workspace.
>
> Read the source idea note referenced in Context above. Read `src/biblio/docling.py` and `src/biblio/grobid.py` to understand the existing derivative pattern. Read `src/biblio/mcp.py` (especially the `paper_context` function), `src/biblio/cli.py`, `src/biblio/config.py`, and `src/biblio/site.py` (to see how it scans for derivatives).
>
> Implement the following:
>
> 1. **Summary module** — Create `src/biblio/summarize.py` with:
>    - `assemble_context(citekey, root)`: gathers paper_context (BibTeX metadata, docling markdown, GROBID header/references) into a structured prompt input. Returns the assembled text.
>    - `summarize(citekey, root, prompt_only=False)`: assembles context, then either returns the prompt (if `prompt_only=True`) or calls LLM to generate a structured summary. Uses Anthropic Python SDK. Skip gracefully if no API key.
>    - Summary output sections: **Contribution**, **Method**, **Key Findings**, **Limitations**, **Relevance** (to project context).
>
> 2. **Summary template** — Define the markdown template for `bib/derivatives/summaries/{citekey}.md` with YAML frontmatter (citekey, date_generated, model_used) and the structured sections.
>
> 3. **CLI command** — Add `biblio summarize <citekey> [--prompt-only] [--status unread] [--force]`. `--prompt-only` prints the assembled prompt to stdout (pipe-friendly). `--status` enables batch mode over papers with that status. `--force` regenerates existing summaries.
>
> 4. **MCP tool** — Add `biblio_summarize(citekey, prompt_only=False)` MCP tool.
>
> 5. **Site integration** — Verify that `site.py` already picks up files from `bib/derivatives/summaries/`. If not, add the glob pattern.
>
> 6. **Tests** — Test context assembly (mock paper_context), summary template generation, `--prompt-only` mode, and batch filtering.
>
> After all changes, run tests with `make test`. Then use `datalad_save` MCP tool to commit with message: "Add paper summary pipeline with prompt-only mode".

## Acceptance Criteria

- [ ] `summarize.py` module with context assembly and LLM summarization
- [ ] Summaries stored as `bib/derivatives/summaries/{citekey}.md` with frontmatter
- [ ] `--prompt-only` mode outputs assembled context without LLM call
- [ ] `biblio summarize` CLI command with batch mode (`--status`)
- [ ] `biblio_summarize` MCP tool exposed
- [ ] LLM call optional — skips gracefully without API key
- [ ] Site builder picks up summary files
- [ ] Tests pass
- [ ] Changes committed via `datalad_save`

## Result

(Filled in after execution)

## Acceptance Criteria

- [ ] 

## Result

Completed in 658.3s (1 steps, model: opus)

Session: `cbab501c-04d4-4a1e-9b5d-adcef160ab20`
Result note: `/storage2/arash/worklog/workflow/captures/20260328-185916-989e5c/note.md`

### Output

```
All done. Here's what was implemented:

**1. `src/biblio/summarize.py`** — New module with:
- `assemble_context(citekey, root)` — gathers BibTeX metadata, GROBID header/references, docling excerpt, and library status into a structured prompt string
- `summarize(citekey, root, prompt_only, force, model)` — assembles context and optionally calls Anthropic API for LLM summary generation. Gracefully skips if no API key or anthropic package missing
- `_render_summary_md()` — renders output with YAML frontmatter (citekey, date_generated, model_used)
- Summary sections: Contribution, Method, Key Findings, Limitations, Relevance

**2. CLI** (`cli.py`) — `biblio summarize <citekey> [--prompt-only] [--status unread] [--force] [--model]`
- `--prompt-only` prints assembled context to stdout (pipe-friendly)
- `--status` enables batch mode filtering by library status
- `--force` regenerates existing summaries

**3. MCP** (`mcp.py`) — `biblio_summarize(citekey, prompt_only, force, model)` function added. The projio server registration (`projio/mcp/biblio.py` + `server.py`) couldn't be written due to write permissions on that directory.

**4. Site integration** (`site.py`) — Updated to also check `bib/derivatives/summaries/{citekey}.md` in addition to the existing docling-dir scan.

**5. Tests** (`tests/test_biblio_summarize.py`) — 7 tests covering context assembly, frontmatter rendering, prompt-only mode, skip-existing behavior, no-API-key handling, batch filtering, and site discovery.

**6. `make test`** passes (21/21 existing tests). New test file was saved but couldn't be run independently due to permission restrictions.

## Next Steps
1. Register `biblio_summarize` in the projio MCP server (`src/projio/mcp/biblio.py` and `server.py`) — needs write access to the projio package.
2. Run the new test file standalone: `PYTHONPATH=src python -m pytest tests/test_biblio_summarize.py -v` to verify all 7 tests pass.
3. Try `biblio summarize --prompt-only --status unread` on a real pape
```

