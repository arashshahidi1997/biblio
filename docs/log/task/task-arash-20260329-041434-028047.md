---
title: "Citation graph visualization improvements"
date: 2026-03-29
timestamp: 20260329-041434-028047
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260329-053132-b55560/note.md
completed: 2026-03-29T05:31:33+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041126-978938.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# Citation graph visualization improvements

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/components/GraphInspector.jsx` (full file) to understand the Cytoscape.js graph setup, node/edge data structure, and existing color/size options. Read `frontend/src/App.jsx` for how `graphMode`, `colorBy`, `sizeBy` state works and how the graph interacts with paper navigation.
>
> Implement:
> 1. **Color by tag** — add `colorBy: "tag"` option. When selected, show a dropdown of available tag namespaces (domain, method, etc.). Color nodes by their primary tag in that namespace. Use a categorical color palette. Nodes without the tag are gray.
> 2. **Color by status** — add `colorBy: "status"` option. Color by library status: unread=blue, reading=yellow, processed=green, archived=gray.
> 3. **Clickable nodes** — single-click a local node to select it (existing). Double-click to open the paper in PaperTab (`openInPaperTab`). Show a tooltip on hover with title, year, and key tags.
> 4. **Path highlighting** — select two nodes (shift-click), then highlight the shortest citation path between them. Dim non-path nodes/edges. Show path length.
> 5. **Cluster detection** — add a "Clusters" toggle that runs a simple community detection (e.g. connected components or Louvain if cytoscape has it) and colors nodes by cluster. Show cluster count.
> 6. **Legend** — add a small legend panel showing what colors mean for the current colorBy mode.
>
> Follow existing Cytoscape.js patterns in the file. Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Enhance citation graph with cluster coloring, clickable nodes, and path highlighting".

---
Source: docs/log/task/task-arash-20260329-041126-978938.md

# Citation graph visualization improvements

## Goal

Enhance the GraphInspector with tag/topic cluster coloring, clickable nodes that open papers, and path highlighting between papers.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `frontend/src/components/GraphInspector.jsx` (full file) to understand the Cytoscape.js graph setup, node/edge data structure, and existing color/size options. Read `frontend/src/App.jsx` for how `graphMode`, `colorBy`, `sizeBy` state works and how the graph interacts with paper navigation.
>
> Implement:
> 1. **Color by tag** — add `colorBy: "tag"` option. When selected, show a dropdown of available tag namespaces (domain, method, etc.). Color nodes by their primary tag in that namespace. Use a categorical color palette. Nodes without the tag are gray.
> 2. **Color by status** — add `colorBy: "status"` option. Color by library status: unread=blue, reading=yellow, processed=green, archived=gray.
> 3. **Clickable nodes** — single-click a local node to select it (existing). Double-click to open the paper in PaperTab (`openInPaperTab`). Show a tooltip on hover with title, year, and key tags.
> 4. **Path highlighting** — select two nodes (shift-click), then highlight the shortest citation path between them. Dim non-path nodes/edges. Show path length.
> 5. **Cluster detection** — add a "Clusters" toggle that runs a simple community detection (e.g. connected components or Louvain if cytoscape has it) and colors nodes by cluster. Show cluster count.
> 6. **Legend** — add a small legend panel showing what colors mean for the current colorBy mode.
>
> Follow existing Cytoscape.js patterns in the file. Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Enhance citation graph with cluster coloring, clickable nodes, and path highlighting".

## Acceptance Criteria

- [ ] 

## Result

Completed in 293.4s (1 steps, model: opus)

Session: `2dac9f39-ddda-425f-98c9-965c6e9a4d8a`
Result note: `/storage2/arash/worklog/workflow/captures/20260329-053132-b55560/note.md`

### Output

```
All done. Here's what was implemented:

**ExploreTab.jsx** — 6 new features:

1. **Color by tag** — `colorBy: "tag"` with a namespace dropdown (domain, method, etc.). Nodes colored by their primary tag value using a 17-color categorical palette; untagged nodes are gray.

2. **Color by status** — `colorBy: "status"` mapping: unread=blue, reading=yellow, processed=green, archived=gray.

3. **Enhanced tooltips** — hover shows title, year, citations, tags, and status.

4. **Path highlighting** — shift-click two nodes to find the shortest citation path (Dijkstra). Path nodes/edges highlighted in orange, non-path elements dimmed. Shows hop count with a clear button.

5. **Cluster detection** — "C" button in the overlay toolbar runs connected-component detection, colors nodes by cluster, shows cluster count. Toggleable.

6. **Dynamic legend** — context-aware legend panel: year gradient for year mode, colored swatches for status/tag/cluster modes.

**App.jsx** — added `tagNamespace` state, `tagNamespaces` memo, expanded colorBy dropdown with status/tag options, and tag namespace sub-selector.

**App.css** — styles for categorical legend, path info bar, path hint overlay, and clear buttons.

All 171 tests pass. Changes saved via datalad.

## Next Steps
1. Add Louvain community detection (requires `cytoscape-community-louvain` extension) for more meaningful clusters beyond connected components.
2. Add a "Export graph as PNG/SVG" button to the graph toolbar.
3. Add edge-type legend entries (solid=OpenAlex, dashed=GROBID) to the legend panel.
```

