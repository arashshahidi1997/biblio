---
title: "ORCID-based author publication import"
date: 2026-03-29
timestamp: 20260329-170716-353700
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260330-031342-f19925/note.md
completed: 2026-03-30T03:13:43+02:00
actionable: true
prompt: ""
source_note: docs/log/task/task-arash-20260329-041128-316771.md
project_primary: biblio
tags: [task]
source: worklog-promote
---

# ORCID-based author publication import

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/openalex.py` and `src/biblio/openalex/openalex_client.py` to understand the existing OpenAlex integration. Read `src/biblio/graph.py` for how graph expansion discovers papers via OpenAlex. Read `src/biblio/ingest.py` for the ingest pipeline.
>
> Implement:
> 1. **OpenAlex author query** — add to `openalex_client.py` or a new `src/biblio/author_search.py`:
>    - `search_by_orcid(orcid)` — calls OpenAlex API: `GET https://api.openalex.org/authors?filter=orcid:{orcid}` to get the author record
>    - `get_author_works(author_id, per_page=100)` — paginated fetch of all works by the author
>    - Return: list of works with doi, title, year, journal, cited_by_count, open_access status
> 2. **CLI** — `biblio add --orcid 0000-0002-1234-5678 [--since YEAR] [--min-citations N]` — previews publications, optional filters, then ingests selected DOIs
> 3. **MCP tool** — `biblio_author_works(orcid)` returning the publication list
> 4. **API endpoints**:
>    - `POST /api/authors/search-orcid` — body: `{orcid}`, returns author info + publication list
>    - `POST /api/authors/import` — body: `{dois: [...]}`, ingests selected papers
> 5. **GUI** — add an "Import by Author" section in ActionsTab or a modal:
>    - ORCID input field
>    - "Search" button → shows author name + affiliation + h-index
>    - Publication table with: checkbox, title, year, journal, citations, already-in-library indicator
>    - Filter controls: year range, min citations
>    - "Import Selected" button
> 6. **Tests** — mock OpenAlex API responses
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add ORCID-based author publication import".

---
Source: docs/log/task/task-arash-20260329-041128-316771.md

# ORCID-based author publication import

## Goal

Given an author's ORCID, pull all their publications from OpenAlex and offer selective import into the library.

## Prompt

> Call `project_context()` and `runtime_conventions()`.
>
> Read `src/biblio/openalex.py` and `src/biblio/openalex/openalex_client.py` to understand the existing OpenAlex integration. Read `src/biblio/graph.py` for how graph expansion discovers papers via OpenAlex. Read `src/biblio/ingest.py` for the ingest pipeline.
>
> Implement:
> 1. **OpenAlex author query** — add to `openalex_client.py` or a new `src/biblio/author_search.py`:
>    - `search_by_orcid(orcid)` — calls OpenAlex API: `GET https://api.openalex.org/authors?filter=orcid:{orcid}` to get the author record
>    - `get_author_works(author_id, per_page=100)` — paginated fetch of all works by the author
>    - Return: list of works with doi, title, year, journal, cited_by_count, open_access status
> 2. **CLI** — `biblio add --orcid 0000-0002-1234-5678 [--since YEAR] [--min-citations N]` — previews publications, optional filters, then ingests selected DOIs
> 3. **MCP tool** — `biblio_author_works(orcid)` returning the publication list
> 4. **API endpoints**:
>    - `POST /api/authors/search-orcid` — body: `{orcid}`, returns author info + publication list
>    - `POST /api/authors/import` — body: `{dois: [...]}`, ingests selected papers
> 5. **GUI** — add an "Import by Author" section in ActionsTab or a modal:
>    - ORCID input field
>    - "Search" button → shows author name + affiliation + h-index
>    - Publication table with: checkbox, title, year, journal, citations, already-in-library indicator
>    - Filter controls: year range, min citations
>    - "Import Selected" button
> 6. **Tests** — mock OpenAlex API responses
>
> Run `make test`. Then use `datalad_save` MCP tool to commit with message: "Add ORCID-based author publication import".

## Acceptance Criteria

- [ ] 

## Result

Completed in 810.8s (1 steps, model: opus)

Session: `c64185df-e5d1-4dd5-b596-d2119145d4b3`
Result note: `/storage2/arash/worklog/workflow/captures/20260330-031342-f19925/note.md`

### Output

```
All done. Here's a summary of what was implemented:

**New file: `src/biblio/author_search.py`**
- `search_by_orcid(cfg, orcid)` — looks up author by ORCID via OpenAlex, returns `AuthorRecord` with name, affiliation, h-index, works/citation counts
- `get_author_works(cfg, author_id, *, per_page, since_year, min_citations)` — paginated fetch of all works with cursor pagination, returns `WorkRecord` list with DOI, title, year, journal, citations, OA status

**CLI: `biblio add orcid <ORCID> [--since YEAR] [--min-citations N]`**
- Previews publications in a formatted table with year, citations, OA status, and library status
- Prompts for confirmation then imports all new papers

**API endpoints:**
- `POST /api/authors/search-orcid` — body: `{orcid, since_year?, min_citations?}`, returns author info + works with `in_library` flags
- `POST /api/authors/import` — body: `{dois: [...]}`, ingests selected papers

**GUI: AuthorImportPanel in ActionsTab**
- ORCID input + optional year/citation filters
- Author info header (name, affiliation, h-index)
- Publication table with checkboxes, title, year, journal, citations, OA, exists/new status
- Select all new / Select none buttons
- Import Selected button

**MCP: `biblio.mcp.author_works()`** — ready for projio MCP server registration

**Tests:** 13 new tests in `tests/test_author_search.py` — all passing (181 total pass, 3 pre-existing failures)

**Note:** The projio MCP server registration (`src/projio/mcp/server.py` and `biblio.py`) needs write access to the parent package to add the `biblio_author_works` tool. The `biblio.mcp.author_works` function is already in place.

## Next Steps
1. Register `biblio_author_works` tool in `projio/src/projio/mcp/server.py` and `projio/src/projio/mcp/biblio.py` (needs write access to the projio package).
2. Build the frontend (`npm run build` in `frontend/`) to bundle the new AuthorImportPanel into `static/app.js`.
3. Test the full flow end-to-end: search a real ORCID in the GUI, verify the 
```

